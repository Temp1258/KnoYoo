1) 新建根仓库
# 设定根目录
$ROOT = 'D:\knoyoo'
mkdir $ROOT -Force | Out-Null
Set-Location $ROOT

git init
git branch -m main
pnpm init   # 一路回车

自检：Get-ChildItem -Name 应看到 .git 和 package.json


2) 配置 workspace 与忽略
@"
packages:
  - "apps/*"
  - "packages/*"
"@ | Set-Content -Encoding utf8 .\pnpm-workspace.yaml

@"
node_modules
dist
target
.vscode
.DS_Store
"@ | Set-Content -Encoding utf8 .\.gitignore

git add .
git commit -m "chore: init repo + workspace"


自检：Get-ChildItem -Name 里有 pnpm-workspace.yaml、.gitignore

3) 生成桌面端（Tauri + React + TS）
mkdir .\apps -Force
cd .\apps
pnpm dlx create-tauri-app@latest desktop


交互选择：

Identifier: com.knoyoo.desktop
Language: TypeScript / JavaScript
UI template: React
UI flavor: TypeScript
Package manager: pnpm（其他默认/回车）

完成后会生成 apps\desktop\...

4) 安装依赖 & 首次运行
cd $ROOT
pnpm install

cd .\apps\desktop
pnpm tauri dev

自检：弹出 Tauri 窗口（Welcome to Tauri + React）= 成功 ✅


先在本地确认 Git 身份（只需一次）
cd D:\knoyoo
git config --global user.name  "Temp1258"
git config --global user.email "diwenliu99@gmail.com"

之后在 GitHub 创建一个空仓库
New Repository → Repository name 填 knoyoo → Visibility 自选（Private/ Public）→
不勾选 “Add a README / .gitignore / license”（保持空仓库）→ Create.

通过HTTPS推送：
cd D:\knoyoo
# 把远端地址换成你自己的（页面上复制）
git remote add origin https://github.com/<你的用户名>/knoyoo.git
# 第一次推送 main 分支，并把 upstream 绑定好
git push -u origin main
首次会弹出浏览器让你登录/授权，按提示完成即可。

看看现在有哪些未提交的文件：
cd D:\knoyoo
git status

预期会看到 apps/desktop 等一堆 Untracked files（未跟踪）。

把新建的桌面端代码加入版本控制并提交：
git add apps
git add pnpm-lock.yaml
git commit -m "feat: add Tauri + React desktop app scaffold"
node_modules 会被 .gitignore 排除，不会提交；这是对的。

推送到 GitHub
git pull
刷新 GitHub 页面


小加固（可选，但推荐）：
1. 给空目录放哨兵（防止未来有空目录不被 Git 记录）：
ni .\apps\.gitkeep -Force | Out-Null
ni .\packages\.gitkeep -Force | Out-Null
git add apps\.gitkeep packages\.gitkeep
git commit -m "chore: keep empty dirs"
git push

2. 忽略更多生成物（已经有 target/dist，再补一个常见缓存）：
'**/.turbo' | Add-Content .gitignore
git add .gitignore
git commit -m "chore: ignore turbo cache"
git push

---------------------------------------------

我已重建并提交了 monorepo 根（package.json、.gitignore、pnpm-workspace.yaml）。
已用 Tauri 脚手架生成 apps/desktop，本地能 pnpm tauri dev 跑起来。
已把 apps/desktop、pnpm-lock.yaml 等提交并推送到 GitHub。
还创建了 .gitkeep 与 .gitignore 补充规则（CRLF 提醒是正常提示，忽略即可）。
✅ 完成了 Sprint 1 / 第 0 里程碑：把桌面端骨架（Tauri + React + TS）跑通，并把仓库同步到 GitHub。

🎯 下一个目标（Sprint 1 的剩余部分）：补齐 packages/core（Rust 核心） 和 packages/shared（TS 共享包），并在桌面端调用，形成“最小连通”。
---------------------------------------------
第 1 步：新建 packages/core（Rust 核心库）：
cd D:\knoyoo
mkdir .\packages -Force
cd .\packages

# 初始化 Rust 库
cargo new core --lib


# 写一个最小可用函数
@'
#[no_mangle]
pub extern "C" fn add(a: i32, b: i32) -> i32 { a + b }
'@ | Set-Content -Encoding utf8 .\core\src\lib.rs

# 确保库类型支持后续 FFI（以后给前端/其他端调用）
$toml = Get-Content .\core\Cargo.toml
$toml += @'
[lib]
crate-type = ["rlib","cdylib","staticlib"]
'@
$toml | Set-Content -Encoding utf8 .\core\Cargo.toml

把包名从 core 改为 knoyoo_core
避免与标准库 core 同名造成歧义。
# 把 Cargo.toml 顶部的 name 改掉
(Get-Content .\Cargo.toml) `
 -replace 'name\s*=\s*"core"', 'name = "knoyoo_core"' `
 | Set-Content -Encoding utf8 .\Cargo.toml

# 确保 [lib] 配置在文件里（如果你之前已加可跳过）
Add-Content .\Cargo.toml @'
[lib]
crate-type = ["rlib","cdylib","staticlib"]
'@

出现了错误，这个错很常见：之前用 Add-Content 又在 Cargo.toml 末尾追加了一次 [lib]，导致 重复的 [lib] 区块。我们直接把 Cargo.toml 覆盖成一份“干净且正确”的内容即可。

现在把 Cargo.toml 覆盖为正确版本：
在 D:\knoyoo\packages\core 里执行这段（整段一次性粘贴）：
@'
[package]
name = "knoyoo_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]
'@ | Set-Content -Encoding utf8 .\Cargo.toml
说明：这会用最小必需配置覆盖 Cargo.toml，不会影响你的 src/lib.rs。
重新编译验证：
cargo clean
cargo build
预期：看到 Finished dev profile [unoptimized + debuginfo]，无红字报错 ✅。

-----------------------------------
目前补齐了补齐 packages/core（Rust 核心），
接下来的小目标是：补齐packages/shared（TS 共享包），并在桌面端调用，形成“最小连通”。


我们在朝什么方向发展？：
当前 Sprint（基础设施连通）：
完成 apps（桌面端）⇄ shared（前端通用）⇄ core（后续算法）的基本骨架与连通，确保多端/多包协作的“基本盘”稳定。

下一个 Sprint（按 PRD 的最小功能）：
1. 本地 SQLite + FTS5 的最小读写与搜索；
2. “记录/检索”的最小界面（一个输入框 + 列表）；
3. 把部分规则/打分放进 core（先放一个假实现），提供调用管道。
----------------------------------------

第 1 步：创建 packages/shared（TS 共享包）
在 PowerShell 执行（整段依次运行即可）：

【

# 1) 新建目录并进入
cd D:\knoyoo\packages
mkdir shared\src -Force
cd shared

# 2) 初始化包
pnpm init

# 3) 写 package.json（最小可用：build + 类型声明）
@'
{
  "name": "@knoyoo/shared",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
'@ | Set-Content -Encoding utf8 package.json

# 4) 写 tsconfig.json
@'
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "declaration": true,
    "outDir": "dist",
    "strict": true,
    "moduleResolution": "Bundler",
    "skipLibCheck": true
  },
  "include": ["src"]
}
'@ | Set-Content -Encoding utf8 tsconfig.json

# 5) 写最小导出函数
@'
export const hello = (name: string) => `Hello, ${name}!`;
'@ | Set-Content -Encoding utf8 src\index.ts

# 6) 安装 TypeScript 并首构建
pnpm add -D typescript @types/node
pnpm run build


】
✅ 自检：看到 dist/ 目录（里有 index.js 和 index.d.ts），无红字报错。


第 2 步：让 desktop 能引入 @knoyoo/shared：
# 1) 回到仓库根，让 workspace 识别到新包并建立 link
cd D:\knoyoo
pnpm install

# 2) 再构建一次 shared（确保最新）
pnpm --filter @knoyoo/shared run build



编辑 D:\knoyoo\apps\desktop\src\App.tsx（用以下最小示例覆盖即可）：
import { useState } from "react";
import { hello } from "@knoyoo/shared";

export default function App() {
  const [name, setName] = useState("KnoYoo");
  return (
    <div style={{ padding: 24 }}>
      <h1>{hello(name)}</h1>
      <input
        value={name}
        onChange={(e) => setName(e.target.value)}
        placeholder="Enter a name..."
        style={{ padding: 8, borderRadius: 8 }}
      />
    </div>
  );
}

运行验证：
cd D:\knoyoo\apps\desktop
pnpm tauri dev
✅ 预期：Tauri 窗口显示 “Hello, KnoYoo!”，输入框改文字，标题跟着变。

发现了问题：
desktop 并没有把 @knoyoo/shared 声明为依赖，所以 Vite 解析不到它。把依赖连上就好。


解决问题开始：
1) 把 shared 作为 desktop 的依赖（workspace link）
# 在仓库根
cd D:\knoyoo

# 给 apps/desktop 增加依赖指向本地 workspace 包
pnpm --filter desktop add @knoyoo/shared@workspace:*

预期：命令会更新 apps/desktop/package.json 的 dependencies，出现：
"@knoyoo/shared": "workspace:*"
备注：用 workspace:* 是 pnpm 的本地工作区链接写法，表示引用同仓库里的包。

2)（可选但推荐）让 shared 持续编译或先手动构建一次
只手动构建一次（简单直接）：
pnpm --filter @knoyoo/shared run build

3) 重新跑桌面端
cd D:\knoyoo\apps\desktop
pnpm tauri dev

自检：窗口应正常启动，不再报 “@knoyoo/shared could not be resolved”。页面看到 Hello, KnoYoo!，输入框改文字，标题跟着变 ✅。


问题解决！

提交并推送（让远端也有 shared）：
cd D:\knoyoo
git add packages/shared apps/desktop/package.json
git commit -m "chore: link @knoyoo/shared to desktop (workspace:*)"
git push

太棒啦，shared → desktop 已经连通 ✅。
我们马上进入**“最小记录/检索（SQLite + FTS5）Demo”**，依旧走小步、每步可自检。

目标：在桌面端输入一条文本保存到 SQLite；再用关键字检索（FTS5 全文搜索）并在界面列表展示。
-----------------------------------------

第 1 步：给 Tauri（Rust）加 SQLite 依赖

在 D:\knoyoo\apps\desktop\src-tauri\Cargo.toml 追加依赖（整段覆盖/追加都行）：
[dependencies]
# … 你现有的依赖保留
rusqlite = { version = "0.31", features = ["bundled", "unlock_notify"] }
解释：bundled 会把 SQLite 源码一并编译，默认启用 FTS5（适合 Windows 开发环境）。
自检：先只编译一次确保依赖能拉下来
cd D:\knoyoo\apps\desktop\src-tauri
cargo build
出现 Finished dev profile … 即通过。


第 2 步：在 Rust 里暴露两条命令：新增 & 搜索
编辑 D:\knoyoo\apps\desktop\src-tauri\src\main.rs，把文件内容替换为下面的最小可用版本（整文件覆盖）：

// src-tauri/src/main.rs
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use rusqlite::{params, Connection};

fn open_db() -> Result<Connection, String> {
    let conn = Connection::open("notes.db").map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          content TEXT NOT NULL
        );
        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts USING fts5(content);
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}

#[tauri::command]
fn add_note(content: String) -> Result<i64, String> {
    let mut conn = open_db()?; // 需要可变
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    // 1) 插入主表
    tx.execute("INSERT INTO notes (content) VALUES (?1)", params![content])
        .map_err(|e| e.to_string())?;
    let id = tx.last_insert_rowid();

    // 2) 同步到 FTS5（不要持有 stmt 变量，直接 execute 就不会和 commit 冲突）
    tx.execute(
        "INSERT INTO notes_fts(rowid, content) \
         SELECT id, content FROM notes WHERE id = ?1",
        params![id],
    )
    .map_err(|e| e.to_string())?;

    tx.commit().map_err(|e| e.to_string())?;
    Ok(id)
}

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<String>, String> {
    let conn = open_db()?;
    let mut stmt = conn
        .prepare("SELECT content FROM notes_fts WHERE notes_fts MATCH ?1 LIMIT 20")
        .map_err(|e| e.to_string())?;
    let rows = stmt
        .query_map([query], |row| row.get::<_, String>(0))
        .map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows {
        out.push(r.map_err(|e| e.to_string())?);
    }
    Ok(out)
}

fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .invoke_handler(tauri::generate_handler![add_note, search_notes])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}


自检（只编译后端）：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

看到 Finished dev profile … 就通过✅。
通过即可（这一步不需要开窗口）。


第 3 步：前端增加最小 UI（新增 & 搜索）
编辑 D:\knoyoo\apps\desktop\src\App.tsx，用下面示例覆盖（保留你之前的 @knoyoo/shared 也行，我把它也顺手显示出来）：

import { useState } from "react";
import { invoke } from "@tauri-apps/api/core";
import { hello } from "@knoyoo/shared";

export default function App() {
  const [name, setName] = useState("KnoYoo");
  const [note, setNote] = useState("");
  const [q, setQ] = useState("");
  const [results, setResults] = useState<string[]>([]);
  const [saving, setSaving] = useState(false);

  async function onSave() {
    if (!note.trim()) return;
    setSaving(true);
    try {
      await invoke<number>("add_note", { content: note });
      setNote("");
    } catch (e) {
      console.error(e);
      alert("保存失败: " + e);
    } finally {
      setSaving(false);
    }
  }

  async function onSearch() {
    try {
      const rows = await invoke<string[]>("search_notes", { query: q });
      setResults(rows);
    } catch (e) {
      console.error(e);
      alert("搜索失败: " + e);
    }
  }

  return (
    <div style={{ padding: 24, color: "#111", fontFamily: "system-ui" }}>
      <h2>{hello(name)}</h2>
      <input
        value={name}
        onChange={(e) => setName(e.target.value)}
        placeholder="Enter a name..."
        style={{ padding: 8, borderRadius: 8, marginBottom: 16 }}
      />

      <hr style={{ margin: "16px 0" }} />

      <h3>新增记录</h3>
      <textarea
        value={note}
        onChange={(e) => setNote(e.target.value)}
        placeholder="输入要保存的文本..."
        rows={3}
        style={{ width: "100%", padding: 8, borderRadius: 8 }}
      />
      <div style={{ marginTop: 8 }}>
        <button onClick={onSave} disabled={saving} style={{ padding: "8px 12px" }}>
          {saving ? "保存中..." : "保存"}
        </button>
      </div>

      <h3 style={{ marginTop: 24 }}>全文搜索（FTS5）</h3>
      <input
        value={q}
        onChange={(e) => setQ(e.target.value)}
        placeholder='关键字或 "短语" 或 a OR b'
        style={{ padding: 8, borderRadius: 8, width: "100%" }}
      />
      <div style={{ marginTop: 8 }}>
        <button onClick={onSearch} style={{ padding: "8px 12px" }}>
          搜索
        </button>
      </div>

      <ul style={{ marginTop: 16 }}>
        {results.map((text, i) => (
          <li key={i} style={{ marginBottom: 8 }}>
            {text}
          </li>
        ))}
      </ul>
    </div>
  );
}

第 4 步：跑起来做功能自检

cd D:\knoyoo\apps\desktop
pnpm tauri dev

在窗口里验证：

1. “新增记录”输入任意文本 → 点“保存”（会创建/更新 notes.db 并写入 FTS5 虚表）。
2. “全文搜索”输入关键字 → 点“搜索” → 下方列表出现匹配文本。
3. 试试短语匹配：输入 "完整 短语"（带引号）或 词A OR 词B。

若搜索时报错，通常是 FTS5 表未建好：重启应用或删除当前目录下的 notes.db 重新试（我们在 open_db() 会自动建表）。

第 5 步：提交并推送

cd D:\knoyoo
git add apps/desktop/src-tauri/Cargo.toml apps/desktop/src-tauri/src/main.rs apps/desktop/src/App.tsx
git commit -m "feat: sqlite + fts5 demo (add & search notes via tauri commands)"
git push

-------------------------------------
🚀 已经把「新增 & 搜索（FTS5）」最小闭环跑通了。

接下来我们把它升级到结构化记录 + 高亮片段，更贴近 PRD 的“记录/检索”：
1. 表结构：id | title | content | created_at
2. FTS5：使用 content='notes', content_rowid='id' + 触发器自动同步
3. 搜索结果：返回 title + snippet，高亮命中词

我把步骤拆得很小，每步都能自检。
-----------------------------------------

第 1 步｜升级数据库结构（Rust 后端）

1) 修改 src-tauri/Cargo.toml
之前已加过 rusqlite 就不用改了；没有的话保留：
[dependencies]
rusqlite = { version = "0.31", features = ["bundled", "unlock_notify"] }

2) 覆盖 src-tauri/src/main.rs
把你现有的内容替换为下面这份（包含表、FTS5、触发器、snippet/bm25 排序）：
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use rusqlite::{params, Connection};
use serde::Serialize;

fn open_db() -> Result<Connection, String> {
    let conn = Connection::open("notes.db").map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        PRAGMA foreign_keys = ON;

        -- 主表：结构化字段
        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          title TEXT NOT NULL,
          content TEXT NOT NULL,
          created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
        );

        -- FTS5 虚表，指向主表（content_rowid/id）
        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts
          USING fts5(title, content, content='notes', content_rowid='id');

        -- 触发器：保持主表与 FTS5 同步
        CREATE TRIGGER IF NOT EXISTS notes_ai AFTER INSERT ON notes BEGIN
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_ad AFTER DELETE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_au AFTER UPDATE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}

#[tauri::command]
fn add_note(title: String, content: String) -> Result<i64, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    tx.execute(
        "INSERT INTO notes (title, content) VALUES (?1, ?2)",
        params![title, content],
    )
    .map_err(|e| e.to_string())?;

    let id = tx.last_insert_rowid();
    tx.commit().map_err(|e| e.to_string())?;
    Ok(id)
}

#[derive(Serialize)]
struct Hit {
    id: i64,
    title: String,
    snippet: String,
}

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;
    let mut stmt = conn.prepare(
        // snippet(表, 列索引或名称, 开始标记, 结束标记, 省略号, 片段词数)
        "SELECT n.id, n.title,
                snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12) AS snip
           FROM notes_fts
           JOIN notes n ON n.id = notes_fts.rowid
          WHERE notes_fts MATCH ?1
          ORDER BY bm25(notes_fts) ASC
          LIMIT 50",
    ).map_err(|e| e.to_string())?;

    let rows = stmt.query_map([query], |row| {
        Ok(Hit {
            id: row.get(0)?,
            title: row.get(1)?,
            snippet: row.get(2)?,
        })
    }).map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows {
        out.push(r.map_err(|e| e.to_string())?);
    }
    Ok(out)
}

fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .invoke_handler(tauri::generate_handler![add_note, search_notes])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}

自检：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

看到 Finished dev profile … 即通过 ✅。
如果你之前已经写入过老结构的 notes.db，想“干净迁移”，可以先关闭应用后删除项目根目录下的 notes.db 再跑（会自动重建）。


第 2 步｜前端表单 & 高亮展示
覆盖 apps/desktop/src/App.tsx（保留 @knoyoo/shared 的 hello 展示）：
import { useState } from "react";
import { invoke } from "@tauri-apps/api/core";
import { hello } from "@knoyoo/shared";

type Hit = { id: number; title: string; snippet: string };

export default function App() {
  const [name, setName] = useState("KnoYoo");

  const [title, setTitle] = useState("");
  const [content, setContent] = useState("");
  const [saving, setSaving] = useState(false);

  const [q, setQ] = useState("");
  const [results, setResults] = useState<Hit[]>([]);

  async function onSave() {
    if (!title.trim() || !content.trim()) return;
    setSaving(true);
    try {
      await invoke<number>("add_note", { title, content });
      setTitle("");
      setContent("");
      alert("已保存！");
    } catch (e) {
      console.error(e);
      alert("保存失败: " + e);
    } finally {
      setSaving(false);
    }
  }

  async function onSearch() {
    try {
      const rows = await invoke<Hit[]>("search_notes", { query: q });
      setResults(rows);
    } catch (e) {
      console.error(e);
      alert("搜索失败: " + e);
    }
  }

  const renderSnippet = (s: string) =>
    ({ __html: s.replaceAll("[mark]", "<mark>").replaceAll("[/mark]", "</mark>") });

  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <h2>{hello(name)}</h2>
      <input
        value={name}
        onChange={(e) => setName(e.target.value)}
        placeholder="Enter a name..."
        style={{ padding: 8, borderRadius: 8, marginBottom: 16 }}
      />

      <hr style={{ margin: "16px 0" }} />

      <h3>新增记录</h3>
      <input
        value={title}
        onChange={(e) => setTitle(e.target.value)}
        placeholder="标题"
        style={{ width: "100%", padding: 8, borderRadius: 8, marginBottom: 8 }}
      />
      <textarea
        value={content}
        onChange={(e) => setContent(e.target.value)}
        placeholder="正文内容..."
        rows={4}
        style={{ width: "100%", padding: 8, borderRadius: 8 }}
      />
      <div style={{ marginTop: 8 }}>
        <button onClick={onSave} disabled={saving} style={{ padding: "8px 12px" }}>
          {saving ? "保存中..." : "保存"}
        </button>
      </div>

      <h3 style={{ marginTop: 24 }}>全文搜索（FTS5）</h3>
      <input
        value={q}
        onChange={(e) => setQ(e.target.value)}
        placeholder='关键字 / "短语" / a OR b'
        style={{ padding: 8, borderRadius: 8, width: "100%" }}
      />
      <div style={{ marginTop: 8 }}>
        <button onClick={onSearch} style={{ padding: "8px 12px" }}>
          搜索
        </button>
      </div>

      <ul style={{ marginTop: 16, lineHeight: 1.6 }}>
        {results.map((hit) => (
          <li key={hit.id} style={{ marginBottom: 12 }}>
            <div style={{ fontWeight: 600 }}>{hit.title}</div>
            <div
              dangerouslySetInnerHTML={renderSnippet(hit.snippet)}
            />
          </li>
        ))}
      </ul>
    </div>
  );
}


自检：
cd D:\knoyoo\apps\desktop
pnpm tauri dev

新建：填“标题 + 正文”点【保存】→ 提示“已保存！”
搜索：输入关键词或 "短语" → 列表显示命中片段，带 <mark> 高亮

自检出问题：保存失败

解决：这是正常现象：你之前已经用“老结构（只有 content 列）”跑过一次，所以磁盘里的 notes.db 里没有 title 列。现在后端换成了新结构（有 title / content / created_at），插入时就报 “table notes has no column named title”。

方案｜删除旧库，自动重建（最省事）
1. 先关掉应用窗口（确保 desktop.exe 退出）。
2. 在 PowerShell 执行：
# 找出 notes.db 的位置（通常在 apps/desktop 目录）
Get-ChildItem -Path D:\knoyoo -Recurse -Filter notes.db
# 删除（把下面路径替换成上一条查到的实际路径，如果就是在 apps\desktop 下，直接这样）
Remove-Item -Force D:\knoyoo\apps\desktop\notes.db

3. 重新运行：
cd D:\knoyoo\apps\desktop
pnpm tauri dev
新库会按你现在的 main.rs 自动建表 + 建 FTS5 + 触发器。再试 保存 → 搜索，OK ✅。

提交代码：
cd D:\knoyoo
git add apps/desktop/src-tauri/src/main.rs apps/desktop/src/App.tsx
git commit -m "feat: structured notes + FTS5 snippet highlight (reset db)"
git push


目前一切顺利！我们继续把“记录/检索”升级成一个可用的小工具：加完整 CRUD + 分页（先不改 DB 路径，等跑通再挪到 AppData）。

A. 后端（Rust / Tauri）增加 CRUD + 分页
在 D:\knoyoo\apps\desktop\src-tauri\src\main.rs 把文件替换为下面版本（保留你已有的表、FTS5、触发器；新增 list / update / delete 三个命令）：

#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use rusqlite::{params, Connection};
use serde::Serialize;

fn open_db() -> Result<Connection, String> {
    let conn = Connection::open("notes.db").map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        PRAGMA foreign_keys = ON;

        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          title TEXT NOT NULL,
          content TEXT NOT NULL,
          created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
        );

        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts
          USING fts5(title, content, content='notes', content_rowid='id');

        CREATE TRIGGER IF NOT EXISTS notes_ai AFTER INSERT ON notes BEGIN
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_ad AFTER DELETE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_au AFTER UPDATE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}

#[tauri::command]
fn add_note(title: String, content: String) -> Result<i64, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;
    tx.execute(
        "INSERT INTO notes (title, content) VALUES (?1, ?2)",
        params![title, content],
    )
    .map_err(|e| e.to_string())?;
    let id = tx.last_insert_rowid();
    tx.commit().map_err(|e| e.to_string())?;
    Ok(id)
}

#[derive(Serialize)]
struct Hit { id: i64, title: String, snippet: String }

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;
    let mut stmt = conn.prepare(
        "SELECT n.id, n.title,
                snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12) AS snip
           FROM notes_fts
           JOIN notes n ON n.id = notes_fts.rowid
          WHERE notes_fts MATCH ?1
          ORDER BY bm25(notes_fts) ASC
          LIMIT 50",
    ).map_err(|e| e.to_string())?;

    let rows = stmt.query_map([query], |row| {
        Ok(Hit { id: row.get(0)?, title: row.get(1)?, snippet: row.get(2)? })
    }).map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows { out.push(r.map_err(|e| e.to_string())?); }
    Ok(out)
}

#[derive(Serialize)]
struct Note { id: i64, title: String, content: String, created_at: String }

/// 分页列表：page 从 1 开始，page_size 默认 10
#[tauri::command]
fn list_notes(page: Option<u32>, page_size: Option<u32>) -> Result<Vec<Note>, String> {
    let page = page.unwrap_or(1).max(1);
    let size = page_size.unwrap_or(10).clamp(1, 100);
    let offset = (page - 1) as i64 * size as i64;

    let conn = open_db()?;
    let mut stmt = conn.prepare(
        "SELECT id, title, content, created_at
           FROM notes
          ORDER BY datetime(created_at) DESC
          LIMIT ?1 OFFSET ?2",
    ).map_err(|e| e.to_string())?;

    let rows = stmt.query_map(params![size as i64, offset], |row| {
        Ok(Note {
            id: row.get(0)?, title: row.get(1)?, content: row.get(2)?, created_at: row.get(3)?,
        })
    }).map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows { out.push(r.map_err(|e| e.to_string())?); }
    Ok(out)
}

#[tauri::command]
fn update_note(id: i64, title: String, content: String) -> Result<(), String> {
    let mut conn = open_db()?;
    conn.execute(
        "UPDATE notes SET title=?1, content=?2 WHERE id=?3",
        params![title, content, id],
    )
    .map_err(|e| e.to_string())?;
    Ok(())
}

#[tauri::command]
fn delete_note(id: i64) -> Result<(), String> {
    let mut conn = open_db()?;
    conn.execute("DELETE FROM notes WHERE id=?1", params![id])
        .map_err(|e| e.to_string())?;
    Ok(())
}

fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .invoke_handler(tauri::generate_handler![
            add_note, search_notes, list_notes, update_note, delete_note
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}

自检：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build
Finished dev profile … 即通过。

B. 前端：列表 + 删除 + 编辑（超简）
把 D:\knoyoo\apps\desktop\src\App.tsx 替换为下面版本（保留搜索高亮；新增“最近 10 条”列表、删除、编辑）：

import { useEffect, useState } from "react";
import { invoke } from "@tauri-apps/api/core";
import { hello } from "@knoyoo/shared";

type Hit = { id: number; title: string; snippet: string };
type Note = { id: number; title: string; content: string; created_at: string };

export default function App() {
  const [name, setName] = useState("KnoYoo");

  const [title, setTitle] = useState("");
  const [content, setContent] = useState("");
  const [editingId, setEditingId] = useState<number | null>(null);
  const [saving, setSaving] = useState(false);

  const [q, setQ] = useState("");
  const [results, setResults] = useState<Hit[]>([]);
  const [list, setList] = useState<Note[]>([]);
  const [page, setPage] = useState(1);

  useEffect(() => { refresh(); }, [page]);

  async function refresh() {
    const rows = await invoke<Note[]>("list_notes", { page, pageSize: 10 });
    setList(rows);
  }

  async function onSave() {
    if (!title.trim() || !content.trim()) return;
    setSaving(true);
    try {
      if (editingId == null) {
        await invoke<number>("add_note", { title, content });
      } else {
        await invoke("update_note", { id: editingId, title, content });
        setEditingId(null);
      }
      setTitle("");
      setContent("");
      await refresh();
      alert("已保存！");
    } catch (e) {
      console.error(e);
      alert("保存失败: " + e);
    } finally {
      setSaving(false);
    }
  }

  async function onEdit(n: Note) {
    setEditingId(n.id);
    setTitle(n.title);
    setContent(n.content);
  }

  async function onDelete(id: number) {
    if (!confirm("确认删除？")) return;
    await invoke("delete_note", { id });
    await refresh();
  }

  async function onSearch() {
    try {
      const rows = await invoke<Hit[]>("search_notes", { query: q });
      setResults(rows);
    } catch (e) {
      console.error(e);
      alert("搜索失败: " + e);
    }
  }

  const renderSnippet = (s: string) =>
    ({ __html: s.replaceAll("[mark]", "<mark>").replaceAll("[/mark]", "</mark>") });

  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <h2>{hello(name)}</h2>
      <input
        value={name} onChange={(e) => setName(e.target.value)}
        placeholder="Enter a name..." style={{ padding: 8, borderRadius: 8, marginBottom: 16 }}
      />

      <hr style={{ margin: "16px 0" }} />

      <h3>{editingId ? "编辑记录" : "新增记录"}</h3>
      <input
        value={title} onChange={(e) => setTitle(e.target.value)} placeholder="标题"
        style={{ width: "100%", padding: 8, borderRadius: 8, marginBottom: 8 }}
      />
      <textarea
        value={content} onChange={(e) => setContent(e.target.value)} placeholder="正文内容..."
        rows={4} style={{ width: "100%", padding: 8, borderRadius: 8 }}
      />
      <div style={{ marginTop: 8, display: "flex", gap: 8 }}>
        <button onClick={onSave} disabled={saving} style={{ padding: "8px 12px" }}>
          {saving ? "保存中..." : (editingId ? "更新" : "保存")}
        </button>
        {editingId && (
          <button onClick={() => { setEditingId(null); setTitle(""); setContent(""); }}>
            取消编辑
          </button>
        )}
      </div>

      <h3 style={{ marginTop: 24 }}>全文搜索（FTS5）</h3>
      <input
        value={q} onChange={(e) => setQ(e.target.value)} placeholder='关键字 / "短语" / a OR b'
        style={{ padding: 8, borderRadius: 8, width: "100%" }}
      />
      <div style={{ marginTop: 8 }}>
        <button onClick={onSearch} style={{ padding: "8px 12px" }}>搜索</button>
      </div>
      <ul style={{ marginTop: 16, lineHeight: 1.6 }}>
        {results.map((hit) => (
          <li key={hit.id} style={{ marginBottom: 12 }}>
            <div style={{ fontWeight: 600 }}>{hit.title}</div>
            <div dangerouslySetInnerHTML={renderSnippet(hit.snippet)} />
          </li>
        ))}
      </ul>

      <h3 style={{ marginTop: 24 }}>最近记录</h3>
      <ul style={{ marginTop: 8 }}>
        {list.map(n => (
          <li key={n.id} style={{ marginBottom: 10 }}>
            <div style={{ fontWeight: 600 }}>{n.title}</div>
            <div style={{ fontSize: 12, opacity: 0.7 }}>{n.created_at}</div>
            <div style={{ marginTop: 6, display: "flex", gap: 8 }}>
              <button onClick={() => onEdit(n)}>编辑</button>
              <button onClick={() => onDelete(n.id)}>删除</button>
            </div>
          </li>
        ))}
      </ul>
      <div style={{ display: "flex", gap: 8 }}>
        <button disabled={page === 1} onClick={() => setPage(p => Math.max(1, p - 1))}>上一页</button>
        <button onClick={() => setPage(p => p + 1)}>下一页</button>
        <button onClick={refresh}>刷新</button>
      </div>
    </div>
  );
}

自检：
cd D:\knoyoo\apps\desktop
pnpm tauri dev
要求效果达到：
1.  新增一条 → 出现在“最近记录”；
2. 点击“编辑”→ 修改并“更新”；
3. 点击“删除”→ 记录消失；
4. “上一页/下一页”能切换分页；
5. 搜索依旧可用并带高亮。

发现中文搜索功能不完善，在全文搜索中输入了中文短语大那是并没有出现搜索出来过的已经保存的结果

这是 FTS5 对中文没有分词 的老问题。默认 tokenizer（unicode61）对英文效果好，但中文因为没有空格，容易“一整个短语当成一个词”或“按单字切分”，导致我输入的“这是”匹配不到期望内容。

为了先可用，我们做一个“混合检索”的落地版：
1. 英文/含空格 → 用 FTS5（bm25 + snippet，高亮和排序都很好）
2. 中文（含 CJK 字符） → 回退到 LIKE 子串匹配（先满足“能搜到”），并在后端生成简单 snippet，仍然返回带 [mark]…[/mark] 的高亮片段
之后再升级为“中文分词器/Bigram 索引”（比如接入结巴或自定义 n-gram 分词）——那是下一阶段。

一次性修改（只动后端 search_notes）
用以下code将 D:\knoyoo\apps\desktop\src-tauri\src\main.rs 全部覆盖：
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use rusqlite::{params, Connection};
use serde::Serialize;

fn open_db() -> Result<Connection, String> {
    let conn = Connection::open("notes.db").map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        PRAGMA foreign_keys = ON;

        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          title TEXT NOT NULL,
          content TEXT NOT NULL,
          created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
        );

        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts
          USING fts5(title, content, content='notes', content_rowid='id');

        CREATE TRIGGER IF NOT EXISTS notes_ai AFTER INSERT ON notes BEGIN
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_ad AFTER DELETE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_au AFTER UPDATE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}

#[tauri::command]
fn add_note(title: String, content: String) -> Result<i64, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;
    tx.execute(
        "INSERT INTO notes (title, content) VALUES (?1, ?2)",
        params![title, content],
    )
    .map_err(|e| e.to_string())?;
    let id = tx.last_insert_rowid();
    tx.commit().map_err(|e| e.to_string())?;
    Ok(id)
}









#[derive(Serialize)]
struct Hit {
    id: i64,
    title: String,
    snippet: String,
}

fn has_cjk(s: &str) -> bool {
    s.chars().any(|c|
        ('\u{4E00}'..='\u{9FFF}').contains(&c)  // CJK Unified
        || ('\u{3400}'..='\u{4DBF}').contains(&c) // Ext-A
        || ('\u{20000}'..='\u{2A6DF}').contains(&c) // Ext-B
        || ('\u{2A700}'..='\u{2B73F}').contains(&c) // Ext-C
        || ('\u{2B740}'..='\u{2B81F}').contains(&c) // Ext-D
        || ('\u{2B820}'..='\u{2CEAF}').contains(&c) // Ext-E
        || ('\u{F900}'..='\u{FAFF}').contains(&c) // CJK Compatibility Ideographs
    )
}

/// 生成一个非常简单的 snippet：命中位置左右各取一些字节（UTF-8 安全：基于 char 处理）
fn make_snippet(text: &str, needle: &str, context: usize) -> String {
    if let Some(pos) = text.find(needle) {
        let start = text[..pos].chars().rev().take(context).collect::<Vec<_>>();
        let end = text[pos + needle.len()..].chars().take(context).collect::<Vec<_>>();
        let mut left = start.into_iter().rev().collect::<String>();
        let mid = format!("[mark]{}[/mark]", needle);
        let mut right = end.into_iter().collect::<String>();
        // 省略号
        if left.chars().count() == context { left = format!("…{}", left); }
        if right.chars().count() == context { right = format!("{}…", right); }
        format!("{left}{mid}{right}")
    } else {
        // 找不到就截取开头
        text.chars().take(context * 2).collect::<String>()
    }
}

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;

    if has_cjk(&query) {
        // 中文：LIKE 回退（标题/正文都搜）
        let like = format!("%{}%", query);
        let mut stmt = conn.prepare(
            "SELECT id, title, content
               FROM notes
              WHERE title LIKE ?1 OR content LIKE ?1
              ORDER BY datetime(created_at) DESC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([like], |row| {
            let id: i64 = row.get(0)?;
            let title: String = row.get(1)?;
            let content: String = row.get(2)?;
            let snippet = make_snippet(&content, &query, 24);
            Ok(Hit { id, title, snippet })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    } else {
        // 英文/带空格：继续用 FTS5（bm25 + snippet）
        let mut stmt = conn.prepare(
            "SELECT n.id, n.title,
                    snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12) AS snip
               FROM notes_fts
               JOIN notes n ON n.id = notes_fts.rowid
              WHERE notes_fts MATCH ?1
              ORDER BY bm25(notes_fts) ASC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([query], |row| {
            Ok(Hit { id: row.get(0)?, title: row.get(1)?, snippet: row.get(2)? })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    }
}









#[derive(Serialize)]
struct Note { id: i64, title: String, content: String, created_at: String }

/// 分页列表：page 从 1 开始，page_size 默认 10
#[tauri::command]
fn list_notes(page: Option<u32>, page_size: Option<u32>) -> Result<Vec<Note>, String> {
    let page = page.unwrap_or(1).max(1);
    let size = page_size.unwrap_or(10).clamp(1, 100);
    let offset = (page - 1) as i64 * size as i64;

    let conn = open_db()?;
    let mut stmt = conn.prepare(
        "SELECT id, title, content, created_at
           FROM notes
          ORDER BY datetime(created_at) DESC
          LIMIT ?1 OFFSET ?2",
    ).map_err(|e| e.to_string())?;

    let rows = stmt.query_map(params![size as i64, offset], |row| {
        Ok(Note {
            id: row.get(0)?, title: row.get(1)?, content: row.get(2)?, created_at: row.get(3)?,
        })
    }).map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows { out.push(r.map_err(|e| e.to_string())?); }
    Ok(out)
}

#[tauri::command]
fn update_note(id: i64, title: String, content: String) -> Result<(), String> {
    let conn = open_db()?;
    conn.execute(
        "UPDATE notes SET title=?1, content=?2 WHERE id=?3",
        params![title, content, id],
    )
    .map_err(|e| e.to_string())?;
    Ok(())
}

#[tauri::command]
fn delete_note(id: i64) -> Result<(), String> {
    let conn = open_db()?;
    conn.execute("DELETE FROM notes WHERE id=?1", params![id])
        .map_err(|e| e.to_string())?;
    Ok(())
}

fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .invoke_handler(tauri::generate_handler![
            add_note, search_notes, list_notes, update_note, delete_note
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}

自检
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

cd D:\knoyoo\apps\desktop
pnpm tauri dev

在中文输入框里搜索，比如：这是、标题、正文本 等，应能搜出；命中词会高亮。
英文（或含空格的查询）仍走 FTS5，排序和片段如之前。

一切正常！

提交：
cd D:\knoyoo
git add apps/desktop/src-tauri/src/main.rs apps/desktop/src/App.tsx
git commit -m "feat: CRUD + pagination; keep FTS5 search with snippet/bm25"
git push

提交成功！！

-------------------------------------
当前进度总结：
已经把「本地知识小本儿」跑成了一个能用的桌面应用。先给一个一眼看懂的里程碑雷达图：
1.  环境：Git / Node 22 + pnpm 10 / Rust stable ✅
2. 仓库：pnpm monorepo（apps + packages），已推到 GitHub ✅
3. 桌面端：apps/desktop（Tauri v2 + React + TS）能启动 ✅
4. 共享包：@knoyoo/shared（TS）已编译并在前端使用 ✅
5. 数据层：SQLite（notes.db）+ FTS5 虚表 + 同步触发器 ✅
6. 后端 API：add_note / search_notes / list_notes / update_note / delete_note ✅
7. 前端功能：新增/编辑/删除/分页列表 + 全文搜索（英文走 FTS5、中文走 LIKE 回退）+ 高亮 ✅

目标分层（总目标 → 阶段目标 → 当前小目标）

总目标：做出 KnoYoo 的“本地知识助理”桌面端雏形：可离线存储、全文检索、后续接 AI Agent。
阶段目标（MVP）：一个可靠的本地应用，具备 CRUD + 中英检索 + 数据放到系统应用目录（便于安装/升级/备份），并且能打包分发。
当前小目标（我们现在所在阶段）：
把数据库从项目目录迁到系统 AppData（更规范，不怕误删、也便于之后打包），保持现有功能不变。
---------------------------------------

下面继续一步一步往前推进。我们只做一件事：把 DB 挪到 AppData。
说明：这里用 directories crate 取到系统应用数据目录（Windows 就是 C:\Users\你\AppData\Roaming\…），自动创建 KnoYoo\Desktop 子目录并放 notes.db。

第 1 步｜在后端加入 directories 依赖
编辑 D:\knoyoo\apps\desktop\src-tauri\Cargo.toml，在 [dependencies] 里加入一行：
directories = "5"
（rusqlite 保持你现在的配置不变）

第 2 步｜修改后端 open_db，让 DB 落到 AppData
把 D:\knoyoo\apps\desktop\src-tauri\src\main.rs 里的 open_db() 替换为下面版本（其它函数保持不变）：

use rusqlite::{params, Connection};
use serde::Serialize;

use directories::ProjectDirs;
use std::path::PathBuf;

fn app_db_path() -> Result<PathBuf, String> {
    // 组织名/应用名你也可以改，这里会得到：%APPDATA%\KnoYoo\Desktop\notes.db
    let proj = ProjectDirs::from("", "KnoYoo", "Desktop")
        .ok_or_else(|| "cannot resolve app data dir".to_string())?;
    let dir = proj.data_dir();
    std::fs::create_dir_all(dir).map_err(|e| e.to_string())?;
    Ok(dir.join("notes.db"))
}

fn open_db() -> Result<Connection, String> {
    let db_path = app_db_path()?;
    let conn = Connection::open(db_path).map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        PRAGMA foreign_keys = ON;

        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          title TEXT NOT NULL,
          content TEXT NOT NULL,
          created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
        );

        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts
          USING fts5(title, content, content='notes', content_rowid='id');

        CREATE TRIGGER IF NOT EXISTS notes_ai AFTER INSERT ON notes BEGIN
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_ad AFTER DELETE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_au AFTER UPDATE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}
小提示（可选迁移旧数据）：如果想把项目目录里旧的 apps/desktop/notes.db 一并搬过去，先关掉应用，然后手动复制到 %APPDATA%\KnoYoo\Desktop\notes.db 覆盖即可。

自检：
# 重新编译后端
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

# 启动应用
cd D:\knoyoo\apps\desktop
pnpm tauri dev

自检点：
1. 新增/编辑/删除/分页正常；
2. 英文/带空格搜索走 FTS5，高亮正常；
3. 中文关键词走 LIKE 回退，也能搜到且高亮；
4. DB 路径应已迁到 AppData：
Write-Output $env:APPDATA
Get-ChildItem "$env:APPDATA\KnoYoo\Desktop\notes.db"

提交：
cd D:\knoyoo
git add apps/desktop/src-tauri/src/main.rs apps/desktop/src-tauri/Cargo.toml
git commit -m "fix: add missing rusqlite/serde imports; app DB in AppData"
git push

Write-Output $env:APPDATA
Get-ChildItem "$env:APPDATA\KnoYoo\Desktop\notes.db"
这个代码报错：
PS D:\knoyoo\apps\desktop> Write-Output $env:APPDATA
C:\Users\wwlie\AppData\Roaming
PS D:\knoyoo\apps\desktop> Get-ChildItem "$env:APPDATA\KnoYoo\Desktop\notes.db"
Get-ChildItem : Cannot find path 'C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\notes.db' because it
does not exist.
At line:1 char:1
+ Get-ChildItem "$env:APPDATA\KnoYoo\Desktop\notes.db"
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (C:\Users\wwlie\...esktop\notes.db:String) [Get-ChildI
   tem], ItemNotFoundException
    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetChildItemCommand

开始修正：
我认为这个是正常的，我们刚把路径迁到 AppData，看到“找不到文件”通常是因为还没触发真正创建（或路径跟我以为的不一样）。下面一步一步把真相“揪出来”，不跳步：

两个快速自检：
1. 运行应用并保存一次（触发写库）
cd D:\knoyoo\apps\desktop
pnpm tauri dev
# 在界面里随便保存一条记录

2. 在另一个 PowerShell 窗口里全盘搜 AppData 下的 notes.db：
Get-ChildItem "$env:APPDATA" -Recurse -Filter notes.db -ErrorAction SilentlyContinue |
  Select-Object FullName
得到：
FullName
--------
C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\data\notes.db
这个就是APPDATA的数据库位置


-------------------------------------------
重新梳理：
🔭 总目标（来自 PRD & 商业计划）：
1. 打造 知遇（KnoYoo）：一个 个人成长导航器
	a. 功能闭环：记录 → 成长树 → 行业树对标 → 周/3月/6月/1年计划 → 复盘 → 报告/激励
	b. 多端：iOS App + 桌面（Win/macOS，Tauri+React）
	c. 技术核心：Rust Core（打分/对标/计划引擎），本地 DB SQLite + FTS5，离线优先，可选云同步
2. 商业目标：ToC 订阅制（Free → Pro），未来扩展到校园/团队合作

🎯 当前阶段目标（Sprint 1–2，前 4 周）
根据 20 周节奏计划：
✅ Monorepo 框架（pnpm workspace + Rust core + Tauri desktop）
✅ apps/desktop 脚手架（React + Tauri）
✅ packages/core（Rust lib 基础函数）
✅ packages/shared（TS SDK，已被前端调用）
✅ SQLite + FTS5 后端接通，Rust API 跑通
✅ 前端 UI 雏形（新增记录 + 搜索展示）
✅ 中文 FTS5 优化，搜索 & 高亮成功
🟨 数据文件迁移到 %APPDATA%，已定位 notes.db 路径，但还未加持久化/迁移逻辑
🟨 git 提交时出现 Cargo.lock / pnpm-lock.yaml 改动未提交，需要清理
验收目标：
1. 本地 DB 创建成功（可验证 notes.db 存在）
2. 新增记录 + 搜索（英文/中文）功能可用
3. GitHub repo 代码库 干净且可复现


🚦 我现在的位置
👉 我处在 Sprint 2（Wk3–Wk4）末尾，也就是“记录/导入 & 检索”的最后阶段。
1. 框架搭建已完成 ✔️
2. 记录 & 检索的核心闭环已基本可跑 ✔️
3. 还差的就是：确认 DB 存储正常、提交代码库保持干净。
一旦这些完成，我就可以进入 Sprint 3（成长树 & 行业树 v1） 的开发。
-------------------------------------------

✅ 下一步行动（建议一步一步做）
Step 1. 清理并提交
cd D:\knoyoo
git add -A
git commit -m "chore: sync lockfiles; ensure DB path migration"
git push

👉 这样可以把 Cargo.lock 和 pnpm-lock.yaml 的修改提交上去，保证团队环境一致。
👉 .db 文件要进 .gitignore（不能提交），否则仓库会越来越臃肿。

Step 2. 最终验证 DB

cd D:\knoyoo\apps\desktop
pnpm tauri dev
已确认：
1. 新增记录、搜索中/英都正常；
2. 不需要再手动检查 DB 文件，只要能保存/搜索就说明路径 OK

把 DB 忽略进 Git（如果还没加过）：
echo *.db >> .gitignore
git add .gitignore
git commit -m "chore: ignore local database files"
git push

------------------------------------
继续按计划把**导出 / 导入（JSONL）**做完。这样我以后备份/迁移数据就不需要碰 notes.db，也不用把数据库上传到 Git。

目标：
1. 一键导出所有笔记到：%APPDATA%\KnoYoo\Desktop\data\notes-export.jsonl
2. 一键导入上述文件（把导出的数据再写回库里）
3. 前端提供两个按钮：导出 / 导入

说明：我刚才查到的 DB 在C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\data\notes.db
导出文件会放在同一目录的 notes-export.jsonl。
------------------------------------
A) 后端：加导出/导入命令
1. 在 Cargo.toml（apps/desktop/src-tauri）加依赖

打开 D:\knoyoo\apps\desktop\src-tauri\Cargo.toml，在 [dependencies] 里新增一行（如果已有就不用加）：
serde_json = "1"

2. 在 src-tauri/src/main.rs 里添加代码
（1）文件顶部 import（若已有相同项则不重复）：
use rusqlite::{params, Connection};
use serde::{Serialize, Deserialize};
use directories::ProjectDirs;
use std::path::{PathBuf};
use std::fs::File;
use std::io::{BufRead, BufReader, Write};

（2）确保这两个函数存在（如果你已经有 app_db_path()，请追加 app_data_dir()）：
fn app_data_dir() -> Result<PathBuf, String> {
    let proj = ProjectDirs::from("", "KnoYoo", "Desktop")
        .ok_or_else(|| "cannot resolve app data dir".to_string())?;
    let dir = proj.data_dir(); // ...\KnoYoo\Desktop\data
    std::fs::create_dir_all(dir).map_err(|e| e.to_string())?;
    Ok(dir.to_path_buf())
}

fn app_db_path() -> Result<PathBuf, String> {
    Ok(app_data_dir()?.join("notes.db"))
}


（3）把导出/导入命令加到 main.rs（放在 fn main() 上面任意合适位置）：
#[derive(Serialize)]
struct ExportResult { path: String, count: u32 }

/// 导出为 JSONL（每行一条）到 %APPDATA%\KnoYoo\Desktop\data\notes-export.jsonl
#[tauri::command]
fn export_notes_jsonl() -> Result<ExportResult, String> {
    let conn = open_db()?;
    let mut stmt = conn
        .prepare("SELECT id, title, content, created_at FROM notes ORDER BY id ASC")
        .map_err(|e| e.to_string())?;
    let mut rows = stmt.query([]).map_err(|e| e.to_string())?;

    let out_path = app_data_dir()?.join("notes-export.jsonl");
    let mut file = File::create(&out_path).map_err(|e| e.to_string())?;
    let mut count = 0u32;

    while let Some(row) = rows.next().map_err(|e| e.to_string())? {
        let id: i64 = row.get(0)?;
        let title: String = row.get(1)?;
        let content: String = row.get(2)?;
        let created_at: String = row.get(3)?;
        let obj = serde_json::json!({
            "id": id, "title": title, "content": content, "created_at": created_at
        });
        writeln!(file, "{}", obj.to_string()).map_err(|e| e.to_string())?;
        count += 1;
    }

    Ok(ExportResult {
        path: out_path.display().to_string(),
        count,
    })
}

#[derive(Deserialize)]
struct InNote { title: String, content: String, created_at: Option<String> }

/// 从 %APPDATA%\KnoYoo\Desktop\data\notes-export.jsonl 导入（追加写入）
#[tauri::command]
fn import_notes_jsonl() -> Result<u32, String> {
    let conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;
    let in_path = app_data_dir()?.join("notes-export.jsonl");

    let file = File::open(&in_path).map_err(|e| format!("open {}: {}", in_path.display(), e))?;
    let reader = BufReader::new(file);
    let mut count = 0u32;

    for line in reader.lines() {
        let line = line.map_err(|e| e.to_string())?;
        if line.trim().is_empty() { continue; }
        // 容忍两种格式：完整对象 {id,title,content,created_at} 或 {title, content, created_at?}
        let v: serde_json::Value = serde_json::from_str(&line).map_err(|e| e.to_string())?;
        let (title, content, created_at) = if v.get("title").is_some() && v.get("content").is_some() {
            let t = v["title"].as_str().unwrap_or_default().to_string();
            let c = v["content"].as_str().unwrap_or_default().to_string();
            let ca = v.get("created_at").and_then(|x| x.as_str()).unwrap_or("").to_string();
            (t, c, ca)
        } else {
            let n: InNote = serde_json::from_value(v).map_err(|e| e.to_string())?;
            (n.title, n.content, n.created_at.unwrap_or_default())
        };

        if created_at.is_empty() {
            tx.execute("INSERT INTO notes (title, content) VALUES (?1, ?2)", params![title, content])
                .map_err(|e| e.to_string())?;
        } else {
            tx.execute(
                "INSERT INTO notes (title, content, created_at) VALUES (?1, ?2, ?3)",
                params![title, content, created_at],
            ).map_err(|e| e.to_string())?;
        }
        count += 1;
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok(count)
}

（4）把新命令注册到 invoke_handler：
.invoke_handler(tauri::generate_handler![
  add_note, search_notes, list_notes, update_note, delete_note,
  export_notes_jsonl, import_notes_jsonl
])


B) 前端：加两个按钮
打开 D:\knoyoo\apps\desktop\src\App.tsx，加两个方法并在 UI 放两个按钮（放在“最近记录”标题旁就行）。示例：
import { invoke } from "@tauri-apps/api/core";
// ...

async function onExport() {
  try {
    const res = await invoke<{ path: string; count: number }>("export_notes_jsonl");
    alert(`已导出 ${res.count} 条到：\n${res.path}`);
  } catch (e: any) {
    alert("导出失败: " + e);
  }
}

async function onImport() {
  try {
    const n = await invoke<number>("import_notes_jsonl");
    alert(`已导入 ${n} 条`);
    await refresh();
  } catch (e: any) {
    alert("导入失败: " + e);
  }
}

在 JSX 中你“最近记录”标题附近加入：
<div style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 16 }}>
  <h3 style={{ margin: 0 }}>最近记录</h3>
  <button onClick={onExport}>导出 JSONL</button>
  <button onClick={onImport}>导入 JSONL</button>
</div>


自检：

# 后端编译
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

# 启动
cd D:\knoyoo\apps\desktop
pnpm tauri dev

发现了错误
现在开始修正：

① export 时把 row.get(..)? 的错误改成字符串（map_err）
rusqlite::Row::get 的错误类型是 rusqlite::Error，而我的函数返回的是 Result<_, String>。
所以这 4 行要改成带 map_err(|e| e.to_string())?：

（思路：? 之前先把错误映射成 String，这样函数签名就匹配了。）
位置： apps/desktop/src-tauri/src/main.rs 的 export_notes_jsonl() 里
let id: i64 = row.get(0)?;
let title: String = row.get(1)?;
let content: String = row.get(2)?;
let created_at: String = row.get(3)?;
替换为：
let id: i64 = row.get(0).map_err(|e| e.to_string())?;
let title: String = row.get(1).map_err(|e| e.to_string())?;
let content: String = row.get(2).map_err(|e| e.to_string())?;
let created_at: String = row.get(3).map_err(|e| e.to_string())?;

② import 时要把连接声明为可变（要开启事务）
conn.transaction() 需要可变引用，所以把 let conn = open_db()?; 改为 let mut conn = open_db()?;
位置： 同文件 import_notes_jsonl() 函数里
let conn = open_db()?;
let tx = conn.transaction().map_err(|e| e.to_string())?;
改为：
let mut conn = open_db()?;
let tx = conn.transaction().map_err(|e| e.to_string())?;

自检：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build
cd D:\knoyoo\apps\desktop
pnpm tauri dev

界面起来后：
1. 随便新增 2~3 条记录；
2. 点“导出 JSONL”，应弹出“已导出 N 条到 …\notes-export.jsonl”；
3. 验证文件存在：
Get-ChildItem "$env:APPDATA\KnoYoo\Desktop\data\notes-export.jsonl"
4. 删一条 → 点“导入 JSONL” → 刷新，数据应恢复。



第四条测试失败：
这个报错和数据库、后端都没关系，就是前端里一个作用域的小坑。因为我把onExport/onImport两个函数写在了组件外部（文件顶部），而 refresh() 是定义在组件 App() 里面。组件外部的函数是拿不到组件内部的 refresh 的，所以触发导入时就抛出：
ReferenceError: refresh is not defined

立即修好（最小改动）：
把 onExport、onImport 移进组件里，放在 refresh 定义之后即可（或让它们接收 refresh 作为参数也行，但移进去最简单）。
把你当前 App.tsx 的顶部那两段删掉，然后在组件里加入这两段：

export default function App() {
  // ... 这里是你的 useState / useEffect / refresh 等
  // 已有的
  useEffect(() => { refresh(); }, [page]);

  async function refresh() {
    const rows = await invoke<Note[]>("list_notes", { page, pageSize: 10 });
    setList(rows);
  }

  // ✅ 移到组件内部
  const onExport = async () => {
    try {
      const res = await invoke<{ path: string; count: number }>("export_notes_jsonl");
      alert(`已导出 ${res.count} 条到：\n${res.path}`);
    } catch (e: any) {
      alert("导出失败: " + e);
    }
  };

  // ✅ 移到组件内部，并在成功后调用 refresh()
  const onImport = async () => {
    try {
      const n = await invoke<number>("import_notes_jsonl");
      await refresh();            // 现在拿得到 refresh 了
      alert(`已导入 ${n} 条`);
    } catch (e: any) {
      alert("导入失败: " + e);
    }
  };

  return (
    // 你的 JSX 保持不变，按钮仍然 onClick={onExport} / onClick={onImport}
  );
}

小贴士：在 React 里，凡是要用到组件状态（如 setList）或组件里函数（如 refresh）的逻辑，默认就把它们写在组件内，最省心。

一切顺利，bug修复！！
提交：
cd D:\knoyoo
git add apps/desktop/src/App.tsx
git commit -m "fix(frontend): move export/import inside component so refresh works"
git push

---------------------------------------
我已经把「笔记 + 全文搜索（含中文 fallback）+ 列表分页 + 增删改 + JSONL 导入/导出」全部跑通并提交了。

下面先把全局计划和我当前所在位置梳理一遍，然后直接进入下一步。

1) 总目标 / 阶段目标 / 当前的位置

总目标（MVP）：做一个本地优先的 KnoYoo 桌面端（Tauri + React），包含
	1. 记录信息（标题/正文）
	2. 全文检索（FTS5，中文可用）
	3. 列表分页浏览、编辑、删除
	4. 数据可导入/导出（JSONL）
	5. 可安装、可升级（打包分发）

阶段拆分
	阶段 A：工程基座 ✅（已完成）
	Workspace、Tauri 模板、运行环境、共享包 @knoyoo/shared。
	
	阶段 B：功能 MVP ✅（已完成）
	SQLite + FTS5、中文搜索 fallback、基础 UI、分页、导入/导出。

	阶段 C：打包 & 分发 ⬅️ 现在就在这里，准备开始
	生成 Windows 安装包（.msi），本地验证安装与运行。

	阶段 D：体验优化（可选）
	快捷键/菜单、托盘、日志 & 错误上报（本地）、自动备份等。

	阶段 E：版本发布与小规模试用（可选）
	版本号规范、CHANGELOG、简单的回收反馈机制。
---------------------------------------

现在要做的事（阶段 C：打包分发 / Windows）
C-1. 基本检查
在 Windows（MSVC toolchain）环境里：
# 确认你在 app 目录
cd D:\knoyoo\apps\desktop
# 先做一次开发编译，确保代码状态 OK
pnpm tauri dev
能正常启动即通过。

C-2. 配置应用信息（图标/名称/版本）
打开 D:\knoyoo\apps\desktop\src-tauri\tauri.conf.json，确认/调整这些字段（示例）：

{
  "productName": "KnoYoo",
  "version": "0.1.0",
  "identifier": "com.knoyoo.desktop",
  "build": {
    "frontendDist": "../dist",
    "beforeDevCommand": "pnpm dev",
    "beforeBuildCommand": "pnpm build"
  },
  "app": {
    "windows": [{ "title": "KnoYoo" }]
  },
  "bundle": {
    "active": true,
    "targets": ["msi"],          // Windows 建议 msi
    "icon": ["icons/icon.ico"],  // 已有图标就沿用；没有可先用模板自带
    "windows": {
      "wix": { "language": "zh-CN" } // 可选：安装器语言
    }
  }
}

更改之后文件代码如下：
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "KnoYoo",
  "version": "0.1.0",
  "identifier": "com.knoyoo.desktop",
  "build": {
    "beforeDevCommand": "pnpm dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "pnpm build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "desktop",
        "width": 800,
        "height": 600
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "targets": ["msi"],
    "icon": ["icons/icon.ico"],
    "windows": {
      "wix": { "language": "zh-CN" }
    }
  }
}

版本号以后每次发版递增（例如 0.1.1 → 0.2.0）。
图标路径可用项目自带 src-tauri/icons/icon.ico，后续再精细化。

C-3. 一键打包
cd D:\knoyoo\apps\desktop
pnpm tauri build



遇到错误：
PS D:\knoyoo\apps\desktop> cd D:\knoyoo\apps\desktop
PS D:\knoyoo\apps\desktop> pnpm tauri build

> desktop@0.1.0 tauri D:\knoyoo\apps\desktop
> tauri "build"

        Info Looking up installed tauri packages to check mismatched versions...
     Running beforeBuildCommand `pnpm build`

> desktop@0.1.0 build D:\KnoYoo\apps\desktop
> tsc && vite build

src/App.tsx:75:18 - error TS2550: Property 'replaceAll' does not exist on type 'string'. Do you need to change your target library? Try changing the 'lib' compiler option to 'es2021' or later.

75     ({ __html: s.replaceAll("[mark]", "<mark>").replaceAll("[/mark]", "</mark>") });
                    ~~~~~~~~~~


Found 1 error in src/App.tsx:75

 ELIFECYCLE  Command failed with exit code 2.
beforeBuildCommand `pnpm build` failed with exit code 2
       Error beforeBuildCommand `pnpm build` failed with exit code 2
 ELIFECYCLE  Command failed with exit code 1.
PS D:\knoyoo\apps\desktop>

咱们已经进入「C. 可执行安装包」阶段，并在 C-3 打包 时遇到了一个前端 TypeScript 编译报错：
Property 'replaceAll' does not exist on type 'string'。这是因为 String.prototype.replaceAll 属于 ES2021，而你当前的 TypeScript 目标/库还是 ES2020。


解决方法A. 提升 TS 目标/库到 ES2021（推荐）
打开 D:\knoyoo\apps\desktop\tsconfig.json，将 compilerOptions 调整为至少 ES2021。最小改动示例：
{
  "compilerOptions": {
    "target": "ES2021",
    "module": "ESNext",
    "declaration": false,
    "outDir": "dist",
    "strict": true,
    "moduleResolution": "Bundler",
    "skipLibCheck": true,
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["src"]
}

改完文件代码如下：
{
  "compilerOptions": {
    "target": "ES2021",
    "useDefineForClassFields": true,
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
说明：lib 加上 ES2021 就会包含 replaceAll 的类型声明；把 target 提到 ES2021 也能让编译产物与之匹配。

重新打包：
cd D:\knoyoo\apps\desktop
pnpm tauri build


成功后的产物与自检
1. 生成的安装包一般在：
D:\knoyoo\apps\desktop\src-tauri\target\release\bundle\msi\
文件名类似：KnoYoo_0.1.0_x64_zh-CN.msi（你设置了 wix.language: zh-CN）。

2. 双击安装后启动应用，自检要点：
	a.  基础读写（新增、编辑、删除、分页）正常；
	b. 中文与英文搜索都能返回结果并高亮；
	c. 导出/导入 JSONL、JSON 正常（导入后列表刷新）；
	d. 数据库仍在 %APPDATA%\KnoYoo\Desktop\data\notes.db，数据不丢。


我们当前的位置（快速回顾）
✅ A：工作区与基础桌面端（Tauri + React）跑通
✅ B：本地 DB（SQLite/FTS5）+ 新增/搜索/分页 + 中英文检索策略 + 导入导出
▶ C：打包发布
	C-1/C-2 已完成（必要依赖与 tauri.conf.json）
	C-3 解决打包编译报错（你现在正在处理，按上面两种方法即可）
	C-4 Windows 安装包自检
处理完本次报错、打包成功后告诉我结果，我们立刻进入 C-4 安装包自检；若还有编译/打包报错，把红字贴出来我一次性帮你排完。


结果：
    Finished `release` profile [optimized] target(s) in 6m 21s
       Built application at: D:\KnoYoo\apps\desktop\src-tauri\target\release\desktop.exe


现在确实只有 release\desktop.exe，但还没看到 bundle\msi\... 里的安装包。
在 Windows + Tauri 的默认流程里，生成 .msi 安装包需要 WiX Toolset。如果机器里没有安装 WiX，Tauri 就会只编译出 .exe，不会产出 .msi。
✅ 解决步骤：
1. 安装 WiX Toolset v3.14（注意是 v3，不是 v4，Tauri 2 目前兼容 v3.14）。
	a. 下载 wix314-binaries.zip 并解压到一个路径，例如：C:\Program Files (x86)\WiX Toolset v3.14\bin。
	b. 把这个 bin 路径加入到系统环境变量 PATH
2. 确认安装成功：
在 PowerShell 输入：
candle.exe -?
light.exe -?
如果能看到版本和帮助说明，说明 OK。
【【【【【【【【【
具体步骤操作如下：
一、下载安装 WiX Toolset v3.14
1. 打开官网发布页：
https://github.com/wixtoolset/wix3/releases/tag/wix314rtm
2. 找到下载项：
	a. wix314.exe → 主安装程序
	b. （可选，我未选）WixToolset.VisualStudioExtension.v3.11.2.vsix → 如果你需要在 Visual Studio 中使用 WiX，可以装这个插件，但 Tauri 不要求。
3. 双击 wix314.exe，一路 Next → Install → Finish。

二、配置环境变量
1. 安装完成后，WiX 默认会放在：
C:\Program Files (x86)\WiX Toolset v3.14\
2. 打开系统环境变量设置：
	a. Win 键 → 输入 环境变量 → 打开 “编辑系统环境变量”
	b. 点击 环境变量 (N)...
3. 在 系统变量 (System variables) 里找到 Path → 编辑 → 新建：
C:\Program Files (x86)\WiX Toolset v3.14\bin
4. 保存退出。

三、验证是否安装成功
1. 打开 PowerShell 或 CMD，输入：
candle -?
如果出现帮助信息（显示 Candle 是 WiX 的编译工具），说明安装成功。 已出现
2. 还可以试：
light -?
这是 WiX 的链接工具，同样会显示帮助。已出现

四、在 Tauri 中使用
1. 只要 candle.exe 和 light.exe 可以在命令行调用，Tauri 打包时就能正常找到 WiX。
2. 打包 Windows MSI 时，Tauri 会自动调用 WiX（只要它在 PATH 环境变量里）。

看输出，一切正常 ✅

快速核对清单（Tauri → MSI）
确认工具链
Rust MSVC toolchain：
rustup show active-toolchain
# 建议是 x86_64-pc-windows-msvc

】】】】】】】】】

我已经把 WiX Toolset v3.14 安装并放进 PATH 了（candle/light 都能显示 3.14.0.8606 的帮助信息）。这就是 Tauri2 在 Windows 打包 MSI 所需要的 WiX 侧前置条件。

接着继续打包！
3. 回到项目目录重新执行：
cd D:\knoyoo\apps\desktop
pnpm tauri build

4. 成功后，会在：
D:\knoyoo\apps\desktop\src-tauri\target\release\bundle\msi\
下生成类似：
KnoYoo_0.1.0_x64_zh-CN.msi

但是依然没有生成
开始查找原因

结论：
1. 现在我拿到的是 可执行文件 desktop.exe（可直接运行），但还没生成安装包。
2. 在 Windows 上要得到 .msi 安装包，Tauri 需要调用 WiX Toolset v3.x。我已经装好了 WiX，但 CLI 目前并没有进入 “打包（bundle）” 阶段，所以没有出现 bundle\msi\...msi。

下面按最可能原因→解决步骤来排查（一次性给到，直接照做即可）。

一步到位的修复与自检：

1. 确保使用的是最新版 CLI
在项目根 D:\knoyoo\apps\desktop 执行：
D:\knoyoo\apps\desktop
pnpm add -D @tauri-apps/cli@latest
（很多“只编译不打包”的情况，就是因为 CLI 版本老或不匹配。）
输出结果为：
update available! 10.15.1 -> 10.16.0
Changelog: https://pnpm.io/v/10.16.0
To update, run: pnpm add -g @pnpm/exe

2. 清理一下 Rust 目标目录（防止旧缓存影响）
cd D:\knoyoo\apps\desktop\src-tauri
cargo clean
输出结果为：
     Removed 8677 files, 7.1GiB total

3. 再次在 app 根目录发起打包，并明确指定目标与打包类型（双保险）
cd D:\knoyoo\apps\desktop
pnpm tauri build --target x86_64-pc-windows-msvc --bundles msi --verbose
	1. --bundles msi 会强制要求产出 MSI；
	2. --verbose 会输出更详细日志，帮我们确认是否调用了 WiX（看有没有 “candle/light” 或 “Bundling Windows MSI” 类信息）。

4. 看看产物是否出现
成功的话，会在这个路径看到安装包：
D:\knoyoo\apps\desktop\src-tauri\target\release\bundle\msi\
  └─ KnoYoo_0.1.0_x64_zh-CN.msi


没有成功，在release文件夹中依然没有出现bundle文件夹
但是好消息：我已经把 MSI 成功打出来了 ✅
从贴的 verbose 日志看，生成物在这里：
D:\KnoYoo\apps\desktop\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\msi\KnoYoo_0.1.0_x64_zh-CN.msi
之所以在 release 目录下没看到 bundle，是因为使用了显式 target（x86_64-pc-windows-msvc），Tauri 会把打包产物放到该目标平台专属子目录下（\target\<triple>\release\bundle\...），而不是普通的 \target\release\bundle\...。

快速打开该目录：
# 打开包含 MSI 的目录
explorer "D:\KnoYoo\apps\desktop\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\msi"

也可以把 MSI 拷贝到更容易找到的位置（例如桌面）后再安装测试。
----------------------
debug结束，加下来计划继续！

我当前进度：C-3 打包 ✅
我们继续做 C-4「安装与冒烟自检」，以及可选的 C-5「发布/签名」：

C-4 安装与冒烟自检（Windows）

1. 安装
	双击 KnoYoo_0.1.0_x64_zh-CN.msi，按向导装好。
2. 冒烟用例（逐条点一下即可）
	1. 能正常启动，窗口标题与 UI 正常显示。
	2. 新增一条记录（标题+正文）→ 刷新/分页可见。
	3. 搜索英文和中文各一次（中文 fallback 的 LIKE 高亮也正常）。
	4. 编辑已有记录 → 内容更新无错。
	5. 删除一条记录 → 列表减少。
	6. 导出 JSON → 弹窗展示路径（%APPDATA%\KnoYoo\Desktop\data\notes-export.json）并存在文件。
	7. 导入 JSON → 成功后列表条数增加。
	8. DB 仍在 %APPDATA%\KnoYoo\Desktop\data\notes.db，可用这条核验：
		Get-Item "$env:APPDATA\KnoYoo\Desktop\data\notes.db"

3. 若要卸载
Windows「应用和功能」里卸载 “KnoYoo”，或运行 MSI 选择卸载。
构建时的 WiX 警告（ICE40/ICE57/ICE61 等）是信息级，默认可忽略；我们后续做签名/安装体验优化时再一起处理。

----------------------------------
已经下载并且做出了本地的测试。
测试结果发现有三个需要修复的问题：
1. 保存英文title和英文content之后，搜索content内容，搜索结果会是： title：title内容 content： title内容 
2. 无法通过数字进行搜索 
3. 在导出和导入之后，重复的内容增加了。感觉导入只是将数据库所有的内容重新增加在数据库中，然后导致了很多的重复。

先卸载软件，准备开始优化。
--------------------------------

要修的 3 个问题 & 修复

1) 搜索“正文”时，结果像是在显示“标题”
原因
FTS5 的命中既可能出现在 title 也可能在 content。我们目前只取了 content 的 snippet；当命中只出现在 title 时，snippet 为空，于是你看到的像是在“用标题当正文”。

修复要点
同时生成两份 snippet：title 和 content。优先展示 content 的命中；若 content 没命中（为空），再回退到 title。

把 search_notes 英文/FTS5 分支的 SQL 改成这样（只改 SQL 这一段即可）：
let mut stmt = conn.prepare(
    "SELECT n.id, n.title,
            -- 两个 snippet：优先 content，没有再用 title
            COALESCE(NULLIF(snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12), ''),
                     snippet(notes_fts, 'title',   '[mark]', '[/mark]', ' … ', 12)) AS snip
       FROM notes_fts
       JOIN notes n ON n.id = notes_fts.rowid
      WHERE notes_fts MATCH ?1
      ORDER BY bm25(notes_fts) ASC
      LIMIT 50"
)?;
这样无论命中出现在标题还是正文，列表都会显示“看起来像正文的一段高亮”。


2) 不能通过数字搜索
原因
数字或非常短的词（如 “1”、“AI”）在 FTS 查询里经常效果不稳定；
而我们中文路径只在包含 CJK 时才走 LIKE 回退，数字并不走这条路。

修复要点
把回退策略放宽：
1. 只要 包含数字/符号 或 长度 ≤ 2，就用 LIKE 路线（标题/正文都搜）。
2. 仍然保留“包含中文 → LIKE”的逻辑。

在 search_notes 顶部加两个小工具函数，然后调整分支：
fn has_digit_or_symbol(s: &str) -> bool {
    s.chars().any(|c| c.is_ascii_digit() || (!c.is_alphabetic() && !c.is_whitespace()))
}

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;

    let need_like = has_cjk(&query) || has_digit_or_symbol(&query) || query.chars().count() <= 2;

    if need_like {
        // 保持你现有的 LIKE 分支（标题/正文都搜，带 snippet(make_snippet)）
        ...
    } else {
        // FTS5 分支里使用上面“修复 1”的 COALESCE 版 SQL
        ...
    }
}

这样“1”、“2024”、“C++”等查询也会正确命中。

改完之后main.rs中的search_notes部分代码如下：

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;

    let need_like = has_cjk(&query) || has_digit_or_symbol(&query) || query.chars().count() <= 2;

    if need_like {
        // 保持你现有的 LIKE 分支（标题/正文都搜，带 snippet(make_snippet)）
        let like = format!("%{}%", query);
        let mut stmt = conn.prepare(
            "SELECT id, title, content
               FROM notes
              WHERE title LIKE ?1 OR content LIKE ?1
              ORDER BY datetime(created_at) DESC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([like], |row| {
            let id: i64 = row.get(0)?;
            let title: String = row.get(1)?;
            let content: String = row.get(2)?;
            let snippet = make_snippet(&content, &query, 24);
            Ok(Hit { id, title, snippet })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    } else {
        // FTS5 分支里使用上面“修复 1”的 COALESCE 版 SQL
        let mut stmt = conn.prepare(
            "SELECT n.id, n.title,
                    -- 两个 snippet：优先 content，没有再用 title
                    COALESCE(NULLIF(snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12), ''),
                             snippet(notes_fts, 'title',   '[mark]', '[/mark]', ' … ', 12)) AS snip
               FROM notes_fts
               JOIN notes n ON n.id = notes_fts.rowid
              WHERE notes_fts MATCH ?1
              ORDER BY bm25(notes_fts) ASC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([query], |row| {
            Ok(Hit { id: row.get(0)?, title: row.get(1)?, snippet: row.get(2)? })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    }
}


3) 导出/导入导致大量重复
原因
我们导出是“全量”，导入是“简单追加”。自然就会重复。

修复要点（两步）：

3.1 建唯一约束，物理去重
在 open_db() 的 execute_batch 里，增加一个 幂等约束（如果你已创建过索引，不会报错）：
CREATE UNIQUE INDEX IF NOT EXISTS idx_notes_dedupe
  ON notes(title, content, created_at);

这三元组基本能唯一定义一条记录；未来如果你要“允许标题相同但正文不同、同时间戳不同毫秒”的更复杂策略，也可以调整组合列。

3.2 导入时用「INSERT OR IGNORE」
把 import_notes_jsonl() 里两处 INSERT 改成 INSERT OR IGNORE（或 INSERT INTO ... ON CONFLICT DO NOTHING，但 SQLite 用前者更直白）：

if created_at.is_empty() {
    tx.execute(
      "INSERT OR IGNORE INTO notes (title, content) VALUES (?1, ?2)",
      params![title, content]
    )?;
} else {
    tx.execute(
      "INSERT OR IGNORE INTO notes (title, content, created_at) VALUES (?1, ?2, ?3)",
      params![title, content, created_at]
    )?;
}

并且在导入函数里统计“忽略数”更直观（可选，已选）
let mut inserted = 0u32;
let mut ignored  = 0u32;

// 每次执行 INSERT OR IGNORE 后：
let changed = tx.changes();
if changed > 0 { inserted += 1; } else { ignored += 1; }

// 最后返回 inserted（或结构体返回 inserted/ignored 都行）


更改之后的import_notes_jsonl():
#[tauri::command]
fn import_notes_jsonl() -> Result<(u32, u32), String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;
    let in_path = app_data_dir()?.join("notes-export.jsonl");

    let file = File::open(&in_path).map_err(|e| format!("open {}: {}", in_path.display(), e))?;
    let reader = BufReader::new(file);
    let mut inserted = 0u32;
    let mut ignored = 0u32;

    for line in reader.lines() {
        let line = line.map_err(|e| e.to_string())?;
        if line.trim().is_empty() { continue; }
        // 容忍两种格式：完整对象 {id,title,content,created_at} 或 {title, content, created_at?}
        let v: serde_json::Value = serde_json::from_str(&line).map_err(|e| e.to_string())?;
        let (title, content, created_at) = if v.get("title").is_some() && v.get("content").is_some() {
            let t = v["title"].as_str().unwrap_or_default().to_string();
            let c = v["content"].as_str().unwrap_or_default().to_string();
            let ca = v.get("created_at").and_then(|x| x.as_str()).unwrap_or("").to_string();
            (t, c, ca)
        } else {
            let n: InNote = serde_json::from_value(v).map_err(|e| e.to_string())?;
            (n.title, n.content, n.created_at.unwrap_or_default())
        };

        if created_at.is_empty() {
            tx.execute(
              "INSERT OR IGNORE INTO notes (title, content) VALUES (?1, ?2)",
              params![title, content]
            )?;
        } else {
            tx.execute(
              "INSERT OR IGNORE INTO notes (title, content, created_at) VALUES (?1, ?2, ?3)",
              params![title, content, created_at]
            )?;
        }
        // 每次执行 INSERT OR IGNORE 后：
        let changed = tx.changes();
        if changed > 0 { inserted += 1; } else { ignored += 1; }
    }

    tx.commit().map_err(|e| e.to_string())?;
    // 最后返回 inserted（或结构体返回 inserted/ignored 都行）
    Ok((inserted, ignored))
}


更改之后的open_db():

fn open_db() -> Result<Connection, String> {
    let db_path = app_db_path()?;
    let conn = Connection::open(db_path).map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        PRAGMA foreign_keys = ON;

        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          title TEXT NOT NULL,
          content TEXT NOT NULL,
          created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
        );

        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts
          USING fts5(title, content, content='notes', content_rowid='id');

        CREATE TRIGGER IF NOT EXISTS notes_ai AFTER INSERT ON notes BEGIN
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_ad AFTER DELETE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_au AFTER UPDATE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE UNIQUE INDEX IF NOT EXISTS idx_notes_dedupe
          ON notes(title, content, created_at);
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}

再次修改：
需要改的文件（3 个）
D:\knoyoo\apps\desktop\src-tauri\src\main.rs
D:\knoyoo\apps\desktop\src-tauri\Cargo.toml
D:\knoyoo\apps\desktop\src\App.tsx


1) 修改 src-tauri/src/main.rs
A. 在 use 区域保持（或补齐）这些导入
use rusqlite::{params, Connection};
use serde::{Serialize, Deserialize};
use directories::ProjectDirs;
use std::path::PathBuf;
use std::fs::File;
use std::io::{BufRead, BufReader, Write};

B. 保持（或替换成）新的数据目录&DB 路径函数
fn app_data_dir() -> Result<PathBuf, String> {
    let proj = ProjectDirs::from("", "KnoYoo", "Desktop")
        .ok_or_else(|| "cannot resolve app data dir".to_string())?;
    let dir = proj.data_dir(); // %APPDATA%\KnoYoo\Desktop\data
    std::fs::create_dir_all(dir).map_err(|e| e.to_string())?;
    Ok(dir.to_path_buf())
}

fn app_db_path() -> Result<PathBuf, String> {
    Ok(app_data_dir()?.join("notes.db"))
}

C. 替换 open_db()：加入去重+唯一索引
把你的 open_db() 函数整体替换为下面版本（先清重复，再建唯一索引，以避免导入后重复）：
fn open_db() -> Result<Connection, String> {
    let db_path = app_db_path()?;
    let conn = Connection::open(db_path).map_err(|e| e.to_string())?;
    conn.execute_batch(
        r#"
        PRAGMA foreign_keys = ON;

        CREATE TABLE IF NOT EXISTS notes (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          title TEXT NOT NULL,
          content TEXT NOT NULL,
          created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
        );

        CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts
          USING fts5(title, content, content='notes', content_rowid='id');

        CREATE TRIGGER IF NOT EXISTS notes_ai AFTER INSERT ON notes BEGIN
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_ad AFTER DELETE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
        END;
        CREATE TRIGGER IF NOT EXISTS notes_au AFTER UPDATE ON notes BEGIN
          INSERT INTO notes_fts(notes_fts, rowid, title, content)
            VALUES('delete', old.id, old.title, old.content);
          INSERT INTO notes_fts(rowid, title, content)
            VALUES (new.id, new.title, new.content);
        END;

        -- 先删除历史重复：相同(title, content, created_at)仅保留最小id那条
        DELETE FROM notes
        WHERE id NOT IN (
          SELECT MIN(id) FROM notes
          GROUP BY title, content, created_at
        );

        -- 再加唯一索引防止后续再产生重复
        CREATE UNIQUE INDEX IF NOT EXISTS idx_notes_dedupe
          ON notes(title, content, created_at);
        "#,
    )
    .map_err(|e| e.to_string())?;
    Ok(conn)
}

D. 新增 判断函数（用于决定是否走 LIKE）
把这个函数加入到 has_cjk 旁边：
fn has_digit_or_symbol(s: &str) -> bool {
    s.chars().any(|c| c.is_ascii_digit() || (!c.is_alphabetic() && !c.is_whitespace()))
}


E. 替换 search_notes()（修复英文 snippet 与数字/短词的检索）
用下面完整版本覆盖你的 search_notes()：
#[derive(Serialize)]
struct Hit {
    id: i64,
    title: String,
    snippet: String,
}

fn has_cjk(s: &str) -> bool {
    s.chars().any(|c|
        ('\u{4E00}'..='\u{9FFF}').contains(&c)  // CJK Unified
        || ('\u{3400}'..='\u{4DBF}').contains(&c) // Ext-A
        || ('\u{20000}'..='\u{2A6DF}').contains(&c) // Ext-B
        || ('\u{2A700}'..='\u{2B73F}').contains(&c) // Ext-C
        || ('\u{2B740}'..='\u{2B81F}').contains(&c) // Ext-D
        || ('\u{2B820}'..='\u{2CEAF}').contains(&c) // Ext-E
        || ('\u{F900}'..='\u{FAFF}').contains(&c) // Compatibility
    )
}

/// 简单 snippet：基于 char，不会切乱码
fn make_snippet(text: &str, needle: &str, context: usize) -> String {
    if let Some(pos) = text.find(needle) {
        let start = text[..pos].chars().rev().take(context).collect::<Vec<_>>();
        let end = text[pos + needle.len()..].chars().take(context).collect::<Vec<_>>();
        let mut left = start.into_iter().rev().collect::<String>();
        let mid = format!("[mark]{}[/mark]", needle);
        let mut right = end.into_iter().collect::<String>();
        if left.chars().count() == context { left = format!("…{}", left); }
        if right.chars().count() == context { right = format!("{}…", right); }
        format!("{left}{mid}{right}")
    } else {
        text.chars().take(context * 2).collect::<String>()
    }
}

#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;

    let need_like = has_cjk(&query) || has_digit_or_symbol(&query) || query.chars().count() <= 2;

    if need_like {
        // 中文/含数字或符号/超短词：回退 LIKE（标题/正文皆搜）
        let like = format!("%{}%", query);
        let mut stmt = conn.prepare(
            "SELECT id, title, content
               FROM notes
              WHERE title LIKE ?1 OR content LIKE ?1
              ORDER BY datetime(created_at) DESC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([like], |row| {
            let id: i64 = row.get(0)?;
            let title: String = row.get(1)?;
            let content: String = row.get(2)?;
            let snippet = make_snippet(&content, &query, 24);
            Ok(Hit { id, title, snippet })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    } else {
        // 英文/可分词：FTS5，优先 content 的 snippet，为空再用 title
        let mut stmt = conn.prepare(
            "SELECT n.id, n.title,
                    COALESCE(NULLIF(snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12), ''),
                             snippet(notes_fts, 'title',   '[mark]', '[/mark]', ' … ', 12)) AS snip
               FROM notes_fts
               JOIN notes n ON n.id = notes_fts.rowid
              WHERE notes_fts MATCH ?1
              ORDER BY bm25(notes_fts) ASC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([query], |row| {
            Ok(Hit { id: row.get(0)?, title: row.get(1)?, snippet: row.get(2)? })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    }
}

F. 替换 import_notes_jsonl()（防重复导入，返回插入/忽略数量）
把你的导入函数整体替换为：
#[derive(Deserialize)]
struct InNote { title: String, content: String, created_at: Option<String> }

#[tauri::command]
fn import_notes_jsonl() -> Result<(u32, u32), String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;
    let in_path = app_data_dir()?.join("notes-export.jsonl");

    let file = File::open(&in_path).map_err(|e| format!("open {}: {}", in_path.display(), e))?;
    let reader = BufReader::new(file);
    let mut inserted = 0u32;
    let mut ignored = 0u32;

    for line in reader.lines() {
        let line = line.map_err(|e| e.to_string())?;
        if line.trim().is_empty() { continue; }

        let v: serde_json::Value = serde_json::from_str(&line).map_err(|e| e.to_string())?;
        let (title, content, created_at) = if v.get("title").is_some() && v.get("content").is_some() {
            let t = v["title"].as_str().unwrap_or_default().to_string();
            let c = v["content"].as_str().unwrap_or_default().to_string();
            let ca = v.get("created_at").and_then(|x| x.as_str()).unwrap_or("").to_string();
            (t, c, ca)
        } else {
            let n: InNote = serde_json::from_value(v).map_err(|e| e.to_string())?;
            (n.title, n.content, n.created_at.unwrap_or_default())
        };

        if created_at.is_empty() {
            tx.execute(
              "INSERT OR IGNORE INTO notes (title, content) VALUES (?1, ?2)",
              params![title, content]
            ).map_err(|e| e.to_string())?;
        } else {
            tx.execute(
              "INSERT OR IGNORE INTO notes (title, content, created_at) VALUES (?1, ?2, ?3)",
              params![title, content, created_at]
            ).map_err(|e| e.to_string())?;
        }

        let changed = tx.changes();
        if changed > 0 { inserted += 1; } else { ignored += 1; }
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok((inserted, ignored))
}
其它已有的 add_note / list_notes / update_note / delete_note / export_notes_jsonl 保持不变即可。


2) 检查/补齐 src-tauri/Cargo.toml
确保依赖区和你现在一致，包含这些（版本与你当前一致即可）：
[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.31", features = ["bundled", "unlock_notify"] }
directories = "5"
你之前因为重复写了 serde_json 报错，这里只保留一行。


3) 修改前端 src/App.tsx
A. 导入保持
import { useEffect, useState } from "react";
import { invoke } from "@tauri-apps/api/core";
import { hello } from "@knoyoo/shared";

B. 调整 onImport：现在后端返回的是二元组 (inserted, ignored)
把你现有的 onImport() 改成下面这样（注意类型和提示文案）：
async function onImport() {
  try {
    const res = await invoke<[number, number]>("import_notes_jsonl");
    const [inserted, ignored] = res;
    alert(`已导入：${inserted} 条；忽略：${ignored} 条（已存在的重复）`);
    await refresh();
  } catch (e: any) {
    alert("导入失败: " + e);
  }
}

C. 搜索小优化（可选）：去掉首尾空格再请求
async function onSearch() {
  try {
    const q2 = q.trim();
    if (!q2) { setResults([]); return; }
    const rows = await invoke<Hit[]>("search_notes", { query: q2 });
    setResults(rows);
  } catch (e) {
    console.error(e);
    alert("搜索失败: " + e);
  }
}
其余 UI 逻辑保持你现状（按钮、列表、snippet 渲染函数等不用动）。


自检步骤（本地运行）

重新编译后端
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

跑应用（开发模式）
cd D:\knoyoo\apps\desktop
pnpm tauri dev

基本优化成功，除了对于英文搜索的问题没有解决：
问题：
搜索英文content内容并不会出现title/content而是出现title/title的内容。
需要继续优化。

原因寻找：
这现象是FTS5 snippet 的列参数传错导致的。
我们在 SQL 里写了：
snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12)
在 FTS5 里第二个参数要的是列的序号（0 开始），不是字符串。这样写会被当作 0，等价于始终取第 0 列（也就是 title），所以你看到“标题/标题”。


路径：D:\knoyoo\apps\desktop\src-tauri\src\main.rs
位置：search_notes 函数的 FTS5 分支（else { ... } 里的 SELECT）
把这一段
let mut stmt = conn.prepare(
    "SELECT n.id, n.title,
            -- 两个 snippet：优先 content，没有再用 title
            COALESCE(NULLIF(snippet(notes_fts, 'content', '[mark]', '[/mark]', ' … ', 12), ''),
                     snippet(notes_fts, 'title',   '[mark]', '[/mark]', ' … ', 12)) AS snip
       FROM notes_fts
       JOIN notes n ON n.id = notes_fts.rowid
      WHERE notes_fts MATCH ?1
      ORDER BY bm25(notes_fts) ASC
      LIMIT 50",
)?;

改成（使用列序号：0=title，1=content）
let mut stmt = conn.prepare(
    "SELECT n.id, n.title,
            /* 优先从 content(1) 抽取 snippet，若空再回退到 title(0) */
            COALESCE(NULLIF(snippet(notes_fts, 1, '[mark]', '[/mark]', ' … ', 12), ''),
                     snippet(notes_fts, 0, '[mark]', '[/mark]', ' … ', 12)) AS snip
       FROM notes_fts
       JOIN notes n ON n.id = notes_fts.rowid
      WHERE notes_fts MATCH ?1
      ORDER BY bm25(notes_fts) ASC
      LIMIT 50",
)?;


为什么现在 OK：snippet(tbl, col, ...) 在 FTS5 的 col 只能用列序号。我们把 1 指向 content，失败才回退到 0（title）。
你的触发器已经把 title/content 都同步写进了 notes_fts，不需要迁移或重建表。


开始自检：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build
cd D:\knoyoo\apps\desktop
pnpm tauri dev

上传git，并打包成msi：
1) 提交到 Git

目标：把刚才对 src-tauri/src/main.rs 和 src/App.tsx 等文件的修改推到 origin/main，同时忽略本地数据库与导入导出文件。
(仅第一次需要) 更新仓库忽略规则，避免把本地 DB/导出文件提交上去：
cd D:\knoyoo
'apps/desktop/src-tauri/notes.db'        | Add-Content .gitignore
'**/notes-export.jsonl'                   | Add-Content .gitignore
'**/notes-export*.json'                   | Add-Content .gitignore
git add .gitignore
git commit -m "chore: ignore local DB/export artifacts"

提交本次代码：
cd D:\knoyoo
# 看看有哪些变更
git status
# 这些文件是本次一定会变的（按我的说明）：
git add apps/desktop/src-tauri/src/main.rs `
        apps/desktop/src/App.tsx `
        apps/desktop/src-tauri/tauri.conf.json
# 如果还改了其它文件（比如 shared 包、样式等），用 git add <path> 一并加上
git commit -m "feat(search/import): fix English snippet, digit search fallback; dedupe import; DB unique index"

git push

✅ 推送成功后，你可以在 GitHub 仓库的 Commits 里看到这条提交。


2) 打包成新的 MSI
目标：生成新的安装包并带上新的版本号（建议小幅 +1，便于区分上一版）。
(可选但强烈建议) bump 版本号
编辑：D:\knoyoo\apps\desktop\src-tauri\tauri.conf.json
{
  "version": "0.1.1",              // ← 把 0.1.0 改成 0.1.1（或更高）
  "bundle": {
    "targets": ["msi"],
    "windows": { "wix": { "language": "zh-CN" } }
  }
}
保存后提交：
cd D:\knoyoo
git add apps/desktop/src-tauri/tauri.conf.json
git commit -m "chore(release): bump version to 0.1.1"
git push

清理旧产物（让产物更纯净）：
cd D:\knoyoo\apps\desktop\src-tauri
cargo clean

打包（指定 x64 + 仅 msi）：
cd D:\knoyoo\apps\desktop
pnpm tauri build --target x86_64-pc-windows-msvc --bundles msi --verbose

打包成功后 MSI 的路径（按 Tauri 默认规则）：
D:\knoyoo\apps\desktop\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\msi\KnoYoo_<版本号>_x64_zh-CN.msi
例如：
D:\knoyoo\apps\desktop\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\msi\KnoYoo_0.1.1_x64_zh-CN.msi
我上次看 target\release 没有 bundle 是因为没传 --bundles msi 或 CLI 自动下载 WiX 之前打的是“裸 exe”。用上面的命令即可在 bundle\msi 下看到 MSI。


3) 安装包快速自检
1. 双击安装 KnoYoo_0.1.1_x64_zh-CN.msi。
2. 打开应用，做这三项冒烟测试：
	1. 新建一条：title="abc 123", content="hello number two" → 保存成功。
	2. 搜索：
		a. 关键词 two：结果 snippet 来自 content（不是 title）
		b. 关键词 123（纯数字）：能返回刚才那条（触发 LIKE 回退）
		c. 中文词（比如“测试”）：也能返回且高亮。
	3. 导出 JSON/导入 JSONL 各一次：导入结果提示“已插入 X 条，忽略 Y 条”，再次导入不会重复新增。

4)打 Git Tag & 生成发布说明
cd D:\knoyoo
git tag v0.1.1 -m "KnoYoo Desktop 0.1.1: search/import fixes"
git push origin v0.1.1
然后到 GitHub → Releases → Draft a new release，选中 v0.1.1，把刚产出的 MSI 上传即可。

---------------------------------------------------------
梳理一下 KnoYoo 当前的开发计划回顾、我们所处的位置，以及接下来的方向：

🔹 总体目标（Business + PRD + 开发节奏）
结合商业计划书与 20周开发节奏：
1. 总目标：发布 KnoYoo v1.0（MVP），跑通最小成长闭环：
	记录 → 检索 → 成长树 → 行业树 v1 → 周/3月计划 → 周报复盘，支持 iOS + 桌面端（Win/macOS），离线优先。
2. 里程碑：
	a. v1.0（Wk1–Wk8）：最小闭环（你当前主要做的阶段）。
	b. v1.1（Wk9–Wk14）：扩展 6月/1年计划、报告增强、导入增强（Notion/Obsidian）、桌面体验优化。
	c. v1.2（Wk15–Wk20）：账号登录、同步（E2EE）、备份与回滚、合规上架。

🔹 当前进展（我已经完成的）
✅ Sprint 1–2（Wk1–Wk4）
1. Monorepo 脚手架搭建，iOS + Desktop 基础骨架 ✅
2. SQLite + FTS5 数据模型 ✅
3. 桌面端（Tauri + React）快速笔记保存/搜索 ✅
4. 导入/导出 JSONL、去重逻辑修复 ✅
5. 搜索问题修复（中文、数字、英文 title/content 显示优化）✅
6. 已完成打包（MSI），并发布到 GitHub Release ✅
📌 说明：这意味着我已经顺利跨过了 Gate A（Wk4） 的检查点 —— “记录/导入/搜索达标”，可以进入下一阶段。

🔹 当前所在阶段
我现在正处在 Sprint 3–4（Wk5–Wk8） 的中期阶段，即：
1. 成长树 & 行业树 v1
2. 差距对标引擎
3. 周/3月计划 + 周报闭环
这一阶段完成后，才算 v1.0 核心闭环跑通。

🔹 下一步开发方向
按照节奏计划，接下来重点是：
Sprint 3（Wk5–Wk6）
1. 成长树可视化
	a. 节点掌握度（0–100）
	b. 短板阈值可设
	c. 渲染性能 ≤300ms
2. 行业树 v1
	a. 40±5 能力项（L1–L5 等级定义）
	b. 能力映射关系编辑 & 回退
3. 对标报告（基础版）
	a. 根据用户笔记/标签 → 行业树能力项 → 计算短板

Sprint 4（Wk7–Wk8）
1. 对标引擎
	a. 差距计算：gap × importance × recency_factor
	b. 输出优先级清单
2. 周/3月计划生成器
	a. 周计划：25–120 分钟任务
	b. 3月计划：3–5 个里程碑
	c. 自动生成 + 可手动调整
3. 周报（简版）
	a. 完成数
	b. 掌握度变化
	c. 连续打卡情况

📌 验收标准（Gate B）：
	a. 完整闭环跑通（记录 → 对标 → 计划 → 周报）
	b. 稳定性达标（崩溃率 <0.5%）
---------------------------------------------------------
更新了开发计划为《Kno Yoo V2 开发节奏计划（更新版）.docx》，让计划更加贴近目标和需求。

明确自己的定位：
当前所处的位置（对齐 20 周节奏）：

1.已完成：Sprint 1–2（Wk1–Wk4）
	Monorepo+桌面端骨架、SQLite+FTS5 接通、最小“记录→检索”闭环、并已打包 MSI 发布到 GitHub Release。
2. 现在：进入 v1.0 的中段——Sprint 3–4（Wk5–Wk8）
	这一阶段聚焦：成长树 & 行业树 v1、对标引擎、以及周/三月计划 + 周报，是跑通 v1.0 最小闭环前的关键路段。	我的《开发日志》已将“当前所在阶段”标注为 Sprint 3–4 中期。
	与计划表对应项：成长树/行业树 v1（40±5 能力项、自动归类、掌握度更新）→ 雷达图 + 周/三月计划 + 周报简	版。
简单说：我已完成“记录/检索”的底座与打包发布，当前正处在 v1.0（Wk5–Wk8）的 Sprint 3–4 阶段，要把“成长树×行业树×固定周期计划”这三件事接上，闭环才算真正成立。
-------------------------------------------------

基于我自己的《开发节奏计划（更新版）》《PRD V2》与现状（已完成“记录/检索 + MSI 发布”），下一步就是把 Sprint 3–4（Wk5–Wk8） 的三件事跑通：成长树 & 行业树 v1、雷达图、周/三月计划 + 周报简版。我把它拆成可直接落地的 8 小步，每步都带“操作—自检—提交”。（步骤严格对齐你文档里的里程碑与验收标准）

接下来要做的事

0）建分支与本地运行
操作
1. 打开 PowerShell，进入仓库根：cd D:\knoyoo
2. 新建分支：git checkout -b feat/sprint3-growth-tree
3. 开发模式启动桌面端：
cd .\apps\desktop
pnpm tauri dev
自检：Tauri 窗口能启动。
自建成功

1）数据库升级：为“成长树 / 行业树 / 掌握度 / 计划”建表
	对齐我的节奏表 Sprint 3–4 的“成长树 & 行业树 v1”“周/三月计划引擎”和“周报简版”的数据面。
操作（Rust/Tauri）
1. 打开 D:\knoyoo\apps\desktop\src-tauri\src\main.rs。
2. 在 open_db() 的 execute_batch 中，在现有 notes/notes_fts 之后追加下面这些表（保持一次性原子创建）：
	a. industry_skill(id, parent_id, name, level, importance)：行业树（Data/AI 方向，v1 先 40±5 能力项；	importance 1–5）
	b. growth_node(id, skill_id, mastery)：你的个人成长树节点与掌握度（0–100）
	c. note_skill_map(note_id, skill_id, weight)：笔记与技能的映射（用于“AI 分类：录入→归类”占位）
	d. plan_task(id, horizon, skill_id, title, minutes, due, status)：计划任务（horizon 取 'WEEK'|'QTR' 先覆盖	“周/三月”）
	e. 视图（可选）：v_skill_gap 计算差距 gap = required_level - mastery（按 PRD/计划里的对标公式先放占位，	importance 参与优先级）
3. 在 main.rs 增加 3 个命令接口（后面步骤会调用）：
	a. seed_industry_v1()：一次性写入 Data/AI 行业树 v1 种子
	b. classify_and_update(note_id)：把一条笔记映射到若干技能，并提升对应 mastery
	c. generate_plan(horizon)：基于差距 + 重要度生成周计划/三月计划（最小可用）
提示：不需要一次写满“最终算法”。v1 先规则法（关键词 ↔ 技能），跑通闭环；v1.1 再切换到更智能的“AI 分类”。这就是你节奏表里“v1.0 占位 → v1.1 接入 AI”的意图。

在编译运行之前，提前顺手优化一下代码：
1. generate_plan 只返回内存向量、并未写入 plan_task 表。如果你希望“计划页签”后面能读出来，建议同时 INSERT 到 plan_task（我下一步会给出补丁）。这符合你 PRD/节奏里“计划引擎”落地。
2. 数据路径注释与实际路径可能不一致：你的注释写的是 %APPDATA%\KnoYoo\Desktop\data，但 directories::ProjectDirs::data_dir() 在 Windows 通常返回 %APPDATA%\KnoYoo\Desktop（没有额外的 \data 这层）。代码没问题，只是注释容易让人误会；下面自检里我会给出“一键打印实际路径”的脚本，确保你看到的就是程序在用的路径。

A. generate_plan 同步写入 plan_task
把现有的 generate_plan 替换为下面版本（仅本函数体变动）——既返回任务列表，又落表，方便后续“计划”页签直接读取。
#[tauri::command]
fn generate_plan(horizon: String) -> Result<Vec<PlanTaskOut>, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    let mut stmt = tx.prepare(
        "SELECT s.id, s.name, s.importance, COALESCE(g.mastery,0) as mastery, s.level
           FROM industry_skill s
           LEFT JOIN growth_node g ON s.id = g.skill_id
          ORDER BY (s.level - COALESCE(g.mastery,0)) * s.importance DESC, s.importance DESC
          LIMIT 5"
    ).map_err(|e| e.to_string())?;

    let mut rows = stmt.query([]).map_err(|e| e.to_string())?;
    let mut out = Vec::new();

    while let Some(row) = rows.next().map_err(|e| e.to_string())? {
        let skill_id: i64 = row.get(0).map_err(|e| e.to_string())?;
        let name: String = row.get(1).map_err(|e| e.to_string())?;
        let importance: i64 = row.get(2).map_err(|e| e.to_string())?;
        let mastery: i64 = row.get(3).map_err(|e| e.to_string())?;
        let level: i64 = row.get(4).map_err(|e| e.to_string())?;
        let gap = level - mastery;
        if gap <= 0 { continue; }

        let minutes = 60 * importance.min(gap); // 简单规则
        let title = format!("提升{}（差距{}）", name, gap);

        // 落库
        tx.execute(
            "INSERT INTO plan_task (horizon, skill_id, title, minutes, due, status)
             VALUES (?1, ?2, ?3, ?4, NULL, 'TODO')",
            rusqlite::params![&horizon, skill_id, &title, minutes],
        ).map_err(|e| e.to_string())?;

        let id = tx.last_insert_rowid();
        out.push(PlanTaskOut {
            id,
            skill_id: Some(skill_id),
            title,
            minutes,
            due: None,
            status: "TODO".to_string(),
        });
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok(out)
}
这样我就可以在前端做一个“生成周计划/三月计划”的按钮：invoke<PlanTaskOut[]>("generate_plan", { horizon: "WEEK" })，并把返回的列表渲染出来，同时也能在需要时重新查询 plan_task 表。

B. 注释与路径统一
我要沿用无 \data 这版路径，只需把代码注释里
// %APPDATA%\KnoYoo\Desktop\data
改成：
// %APPDATA%\KnoYoo\Desktop
避免日后团队困惑。
我确实想要多一层 \data，把：
let dir = proj.data_dir(); 
std::fs::create_dir_all(dir)?;
Ok(dir.to_path_buf())
改成：
let base = proj.data_dir();
let dir = base.join("data");
std::fs::create_dir_all(&dir)?;
Ok(dir)


自检：cd src-tauri && cargo build 成功。
# 切到 tauri 后端目录
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

编译出现问题：
PS D:\knoyoo\apps\desktop> cd D:\knoyoo\apps\desktop\src-tauri
PS D:\knoyoo\apps\desktop\src-tauri> cargo build
   Compiling desktop v0.1.0 (D:\knoyoo\apps\desktop\src-tauri)
error[E0282]: type annotations needed
   --> src\main.rs:394:10
    |
394 |         (None, "数据分析", 1, 5),
    |          ^^^^ cannot infer type of the type parameter `T` declared on the enum `Option`
    |
help: consider specifying the generic argument
    |
394 |         (None::<T>, "数据分析", 1, 5),
    |              +++++

error[E0505]: cannot move out of `tx` because it is borrowed
   --> src\main.rs:515:5
    |
472 |     let tx = conn.transaction().map_err(|e| e.to_string())?;
    |         -- binding `tx` declared here
473 |
474 |     let mut stmt = tx.prepare(
    |                    -- borrow of `tx` occurs here
...
515 |     tx.commit().map_err(|e| e.to_string())?;
    |     ^^ move out of `tx` occurs here
516 |     Ok(out)
517 | }
    | - borrow might be used here, when `stmt` is dropped and runs the `Drop` code for type `Statement`

Some errors have detailed explanations: E0282, E0505.
For more information about an error, try `rustc --explain E0282`.
error: could not compile `desktop` (bin "desktop") due to 2 previous errors
PS D:\knoyoo\apps\desktop\src-tauri>

解决中：
现在遇到的是两个标准的 Rust 报错：
error[E0282]: type annotations needed（None 的类型推断不出来）
error[E0505]: cannot move out of tx because it is borrowed（tx 在被 stmt 借用期间就 commit 了）
下面给出最小补丁（只改两处代码），然后是PowerShell 自检脚本（已按你新路径 %APPDATA%\KnoYoo\Desktop\data\notes.db 调整）。这些工作与我们文档里的 Sprint 3–4 目标一致（成长树/行业树 v1、计划引擎），并且路径也与你的“运行说明”一致。

A. 修复报错 E0282（给 None 明确类型）

问题出在 seed_industry_v1() 的这行：
let skills = vec![
    (None, "数据分析", 1, 5),
    // ...
];
None 是泛型，需要告诉编译器它是 Option<i64>。最干净的方式是给 skills 明确类型注解：

请把 seed_industry_v1() 函数中声明 skills 的那几行替换为下面这一段（仅替换 let skills = ... 相关代码，其它不动）：
// 示例种子（可后续扩充/调整）
let skills: Vec<(Option<i64>, &'static str, i64, i64)> = vec![
    (None, "数据分析",   1, 5),
    (None, "机器学习",   1, 5),
    (None, "深度学习",   1, 4),
    (None, "数据工程",   1, 4),
    (None, "AI 产品",    1, 4),
    (None, "大模型",     1, 4),
    (None, "Prompt 工程",1, 3),
    (None, "数据可视化", 1, 3),
    (None, "数据治理",   1, 3),
    (None, "NLP",       1, 3),
    (None, "CV",        1, 3),
    (None, "推荐系统",   1, 3),
    (None, "知识图谱",   1, 2),
    (None, "AI 安全",    1, 2),
    (None, "AI 法律伦理",1, 2),
];
说明：也可以在每个元素写 None::<i64>，但统一给 Vec 类型注解更简洁。


B. 修复报错 E0505（tx 被借用期间 commit）
问题原因：let mut stmt = tx.prepare(...) 会让 stmt 借用 事务 tx，直到 stmt 被销毁（drop）。你在 tx.commit() 时，stmt 仍在作用域内，编译器因此报错。
解决：在 commit 前先让 stmt 离开作用域，最简单是包一层花括号 { ... }，或显式 drop(stmt)。

请把 generate_plan 函数体替换为下面版本（逻辑与之前一致，只是把“查询与 while 循环”放进一个小作用域，保证在 commit 前 stmt 被释放）：
#[tauri::command]
fn generate_plan(horizon: String) -> Result<Vec<PlanTaskOut>, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    let mut out = Vec::new();

    {
        let mut stmt = tx.prepare(
            "SELECT s.id, s.name, s.importance, COALESCE(g.mastery,0) as mastery, s.level
               FROM industry_skill s
               LEFT JOIN growth_node g ON s.id = g.skill_id
              ORDER BY (s.level - COALESCE(g.mastery,0)) * s.importance DESC, s.importance DESC
              LIMIT 5"
        ).map_err(|e| e.to_string())?;

        let mut rows = stmt.query([]).map_err(|e| e.to_string())?;

        while let Some(row) = rows.next().map_err(|e| e.to_string())? {
            let skill_id: i64 = row.get(0).map_err(|e| e.to_string())?;
            let name: String = row.get(1).map_err(|e| e.to_string())?;
            let importance: i64 = row.get(2).map_err(|e| e.to_string())?;
            let mastery: i64 = row.get(3).map_err(|e| e.to_string())?;
            let level: i64 = row.get(4).map_err(|e| e.to_string())?;
            let gap = level - mastery;
            if gap <= 0 { continue; }

            let minutes = 60 * importance.min(gap); // 简单规则
            let title = format!("提升{}（差距{}）", name, gap);

            // 落库
            tx.execute(
                "INSERT INTO plan_task (horizon, skill_id, title, minutes, due, status)
                 VALUES (?1, ?2, ?3, ?4, NULL, 'TODO')",
                rusqlite::params![&horizon, skill_id, &title, minutes],
            ).map_err(|e| e.to_string())?;

            let id = tx.last_insert_rowid();
            out.push(PlanTaskOut {
                id,
                skill_id: Some(skill_id),
                title,
                minutes,
                due: None,
                status: "TODO".to_string(),
            });
        } // <- 这里 rows 和 stmt 会被自动 drop
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok(out)
}
备选写法：在 commit 之前加一行 drop(stmt); 也可以，但用作用域更稳。


C. PowerShell 自检（已按“带 \data 的新路径”改好）
目标：清理旧库 → 编译 → 运行 →（可选）种子写入 → 生成计划。路径与你的运行说明一致：C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\data\notes.db。

1. 清理旧 DB，防止历史结构干扰
# 1. 计算数据目录与 DB 路径（与你的 main.rs 一致）
$AppData = $env:APPDATA
$DbDir   = Join-Path $AppData "KnoYoo\Desktop\data"
$DbPath  = Join-Path $DbDir   "notes.db"

"DB 目录: $DbDir"
"DB 路径:  $DbPath"

# 2. 确保目录存在
New-Item -ItemType Directory -Force -Path $DbDir | Out-Null

# 3. 如果存在旧库，先删掉（可选但推荐）
if (Test-Path $DbPath) { Remove-Item -Force $DbPath }

2. 重新编译后端
cd D:\knoyoo\apps\desktop\src-tauri
cargo build
预期：Finished dev profile ... 无红字 ✅。

powershell报错：
PS D:\knoyoo\apps\desktop\src-tauri> # 1. 计算数据目录与 DB 路径（与你的 main.rs 一致）

PS D:\knoyoo\apps\desktop\src-tauri> $AppData = $env:APPDATA
PS D:\knoyoo\apps\desktop\src-tauri> $DbDir   = Join-Path $AppData "KnoYoo\Desktop\data"
PS D:\knoyoo\apps\desktop\src-tauri> $DbPath  = Join-Path $DbDir   "notes.db"
PS D:\knoyoo\apps\desktop\src-tauri>
PS D:\knoyoo\apps\desktop\src-tauri> "DB 目录: $DbDir"
DB 目录: C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\data
PS D:\knoyoo\apps\desktop\src-tauri> "DB 路径:  $DbPath"
DB 路径:  C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\data\notes.db
PS D:\knoyoo\apps\desktop\src-tauri>
PS D:\knoyoo\apps\desktop\src-tauri> # 2. 确保目录存在
PS D:\knoyoo\apps\desktop\src-tauri> New-Item -ItemType Directory -Force -Path $DbDir | Out-Null
PS D:\knoyoo\apps\desktop\src-tauri>
PS D:\knoyoo\apps\desktop\src-tauri> # 3. 如果存在旧库，先删掉（可选但推荐）
PS D:\knoyoo\apps\desktop\src-tauri> if (Test-Path $DbPath) { Remove-Item -Force $DbPath }
PS D:\knoyoo\apps\desktop\src-tauri> cd D:\knoyoo\apps\desktop\src-tauri
PS D:\knoyoo\apps\desktop\src-tauri> cargo build
   Compiling desktop v0.1.0 (D:\knoyoo\apps\desktop\src-tauri)
error[E0596]: cannot borrow `conn` as mutable, as it is not declared as mutable
   --> src\main.rs:408:14
    |
408 |     let tx = conn.transaction().map_err(|e| e.to_string())?;
    |              ^^^^ cannot borrow as mutable
    |
help: consider changing this to be mutable
    |
407 |     let mut conn = open_db()?;
    |         +++

For more information about this error, try `rustc --explain E0596`.
error: could not compile `desktop` (bin "desktop") due to 1 previous error
PS D:\knoyoo\apps\desktop\src-tauri>

好消息：现在只剩一个编译错误，原因非常明确、修起来只要一行。
1) 修复本次 cargo build 报错
error[E0596]: cannot borrow `conn` as mutable, as it is not declared as mutable
   --> src\main.rs:408:14
    |
408 |     let tx = conn.transaction().map_err(|e| e.to_string())?;
    |              ^^^^ cannot borrow as mutable
help: consider changing this to be mutable
407 |     let mut conn = open_db()?;

原因
rusqlite::Connection::transaction(&mut self) 需要可变借用，但我在 seed_industry_v1() 里写的是：
let conn = open_db()?;
这会让 conn 变成不可变绑定，导致上面错误。

最小补丁（只改一行）
在 seed_industry_v1() 内，把这一行：
let conn = open_db()?;
改成：
let mut conn = open_db()?;
其余地方已经处理得很好：generate_plan 里用作用域 { ... } 提前 drop 掉 stmt，避免 tx.commit() 的借用冲突，这点是完全正确的。👍

2）改完后的自检步骤（PowerShell）
你的 app_data_dir() 已经固定到 %APPDATA%\KnoYoo\Desktop\data\notes.db，下面的路径和命令完全按这个布局来。

A. 清理旧库（可选但推荐）
$AppData = $env:APPDATA
$DbDir   = Join-Path $AppData "KnoYoo\Desktop\data"
$DbPath  = Join-Path $DbDir   "notes.db"

"DB 目录: $DbDir"
"DB 路径:  $DbPath"

New-Item -ItemType Directory -Force -Path $DbDir | Out-Null
if (Test-Path $DbPath) { Remove-Item -Force $DbPath }

B. 重新编译后端
cd D:\knoyoo\apps\desktop\src-tauri
cargo build
预期：Finished dev profile ... 无红字 ✅

C. 运行开发模式
cd D:\knoyoo\apps\desktop
pnpm tauri dev

预期：Tauri 窗口正常启动。此时数据库会在
C:\Users\<你>\AppData\Roaming\KnoYoo\Desktop\data\notes.db 自动创建


太好了，C 都跑通了！按照节奏，我们现在进入 D：计划引擎接入 + 计划页（最小可用）。目标：一键写入行业树种子 → 生成「周/三月」计划 → 前端展示与勾选完成。这正对应我 v1.0 的 Sprint 3–4 交付（成长树/行业树 v1 + 周/三月计划）。
下面我一步一步做，包含具体改哪些文件、插到哪里、以及 PowerShell 自检。


第 1 步｜Rust 后端：补两个命令（读取与更新计划）
我后端已经能「生成并落库 plan_task」。现在补：
1. list_plan_tasks(horizon, status)：列出计划任务
2. update_plan_status(id, status)：把任务标记为 DONE/ TODO

打开 D:\knoyoo\apps\desktop\src-tauri\src\main.rs，在 generate_plan 下方 新增 下列代码（整段粘贴即可；不覆盖你已有的内容）：

#[derive(Serialize)]
struct PlanTask {
    id: i64,
    skill_id: Option<i64>,
    title: String,
    minutes: i64,
    due: Option<String>,
    status: String,
    horizon: String,
}

#[tauri::command]
fn list_plan_tasks(horizon: Option<String>, status: Option<String>) -> Result<Vec<PlanTask>, String> {
    let conn = open_db()?;
    let (mut sql, mut args): (String, Vec<(usize, String)>) = (
        "SELECT id, skill_id, title, minutes, due, status, horizon
           FROM plan_task".to_string(),
        vec![]
    );
    let mut where_clause: Vec<String> = vec![];
    if let Some(h) = horizon.as_ref() {
        where_clause.push("horizon = ?1".to_string());
        args.push((1, h.clone()));
    }
    if let Some(s) = status.as_ref() {
        where_clause.push(format!("status = ?{}", args.len() + 1));
        args.push((args.len() + 1, s.clone()));
    }
    if !where_clause.is_empty() {
        sql.push_str(" WHERE ");
        sql.push_str(&where_clause.join(" AND "));
    }
    sql.push_str(" ORDER BY COALESCE(due,'9999-12-31') ASC, id DESC LIMIT 200");

    let mut stmt = conn.prepare(&sql).map_err(|e| e.to_string())?;
    let rows = stmt.query_map(
        rusqlite::params_from_iter(args.iter().map(|(_, v)| v)),
        |row| Ok(PlanTask {
            id: row.get(0)?, skill_id: row.get(1)?,
            title: row.get(2)?, minutes: row.get(3)?,
            due: row.get(4)?, status: row.get(5)?, horizon: row.get(6)?,
        })
    ).map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows { out.push(r.map_err(|e| e.to_string())?); }
    Ok(out)
}

#[tauri::command]
fn update_plan_status(id: i64, status: String) -> Result<(), String> {
    let mut conn = open_db()?;
    conn.execute(
        "UPDATE plan_task SET status=?1 WHERE id=?2",
        rusqlite::params![status, id],
    ).map_err(|e| e.to_string())?;
    Ok(())
}

再把 invoke_handler! 里的命令列表追加这两个：
.invoke_handler(tauri::generate_handler![
  add_note, search_notes, list_notes, update_note, delete_note,
  export_notes_jsonl, import_notes_jsonl, seed_industry_v1,
  classify_and_update, generate_plan,
  list_plan_tasks, update_plan_status   // <= 新增这行
])

自检（后端编译）：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

出现 Finished dev profile ... ✅。



第 2 步｜前端：计划区块（初始化行业树 + 生成计划 + 列表 + 勾选）
打开 D:\knoyoo\apps\desktop\src\App.tsx。在「最近记录」区块下面，新增一个 “计划” 区块 UI。可以直接把下面整段插到文件末尾 </div> 之前；或按你布局习惯调整位置。
// ---- 计划区块（新增） ----
import { useEffect, useState } from "react";
import { invoke } from "@tauri-apps/api/core";

// 放在组件里其它 state 后面
type PlanTask = {
  id: number;
  skill_id?: number | null;
  title: string;
  minutes: number;
  due?: string | null;
  status: string;
  horizon: string;
};

const [plans, setPlans] = useState<PlanTask[]>([]);
const [planHorizon, setPlanHorizon] = useState<"WEEK" | "QTR">("WEEK");
const [planFilter, setPlanFilter] = useState<"ALL" | "TODO" | "DONE">("ALL");

async function loadPlans(h?: "WEEK" | "QTR", s?: "ALL" | "TODO" | "DONE") {
  const horizon = h ?? planHorizon;
  const status = (s ?? planFilter) === "ALL" ? null : (s ?? planFilter);
  const rows = await invoke<PlanTask[]>("list_plan_tasks", {
    horizon, status
  });
  setPlans(rows);
}

async function onSeedIndustry() {
  const n = await invoke<number>("seed_industry_v1");
  alert(`行业树种子写入：${n} 项`);
}

async function onGenerate(h: "WEEK" | "QTR") {
  await invoke<PlanTask[]>("generate_plan", { horizon: h });
  await loadPlans(h, planFilter);
  alert(h === "WEEK" ? "已生成周计划" : "已生成三月计划");
}

async function toggleDone(t: PlanTask) {
  const next = t.status === "DONE" ? "TODO" : "DONE";
  await invoke("update_plan_status", { id: t.id, status: next });
  await loadPlans();
}

useEffect(() => { loadPlans(); }, []);

// 在 JSX 里，放在“最近记录”模块之后：
<h3 style={{ marginTop: 24 }}>计划</h3>
<div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 8 }}>
  <button onClick={onSeedIndustry}>初始化行业树种子</button>
  <button onClick={() => onGenerate("WEEK")}>生成周计划</button>
  <button onClick={() => onGenerate("QTR")}>生成三月计划</button>

  <select
    value={planHorizon}
    onChange={(e) => { const v = e.target.value as "WEEK" | "QTR"; setPlanHorizon(v); loadPlans(v, planFilter); }}
    style={{ marginLeft: 12 }}
  >
    <option value="WEEK">WEEK</option>
    <option value="QTR">QTR</option>
  </select>

  <select
    value={planFilter}
    onChange={(e) => { const v = e.target.value as "ALL"|"TODO"|"DONE"; setPlanFilter(v); loadPlans(planHorizon, v); }}
  >
    <option value="ALL">全部</option>
    <option value="TODO">TODO</option>
    <option value="DONE">DONE</option>
  </select>

  <button onClick={() => loadPlans()}>刷新</button>
</div>

<ul style={{ marginTop: 8 }}>
  {plans.map(t => (
    <li key={t.id} style={{ marginBottom: 10, display: "flex", gap: 8, alignItems: "center" }}>
      <input type="checkbox" checked={t.status === "DONE"} onChange={() => toggleDone(t)} />
      <div style={{ flex: 1 }}>
        <div style={{ fontWeight: 600 }}>{t.title}</div>
        <div style={{ fontSize: 12, opacity: 0.7 }}>
          {t.horizon} · {t.minutes} 分钟{t.due ? ` · 截止 ${t.due}` : ""}
        </div>
      </div>
      <span style={{
        fontSize: 12,
        padding: "2px 6px",
        borderRadius: 6,
        background: t.status === "DONE" ? "#e6ffed" : "#fffbe6",
        border: "1px solid #ddd"
      }}>
        {t.status}
      </span>
    </li>
  ))}
</ul>
这段代码用到我后端已经有的 seed_industry_v1 与 generate_plan，再配合我们刚加的 list_plan_tasks / update_plan_status，就能把初始化→生成→展示→勾选串起来。和 PRD 的“固定周期计划：周/三月（v1.0）”一致。

app.tsx在修改完代码如下：
import { useEffect, useState } from "react";
import { invoke } from "@tauri-apps/api/core";
import { hello } from "@knoyoo/shared";



type Hit = { id: number; title: string; snippet: string };
type Note = { id: number; title: string; content: string; created_at: string };

export default function App() {
  const [name, setName] = useState("KnoYoo");

  const [title, setTitle] = useState("");
  const [content, setContent] = useState("");
  const [editingId, setEditingId] = useState<number | null>(null);
  const [saving, setSaving] = useState(false);

  const [q, setQ] = useState("");
  const [results, setResults] = useState<Hit[]>([]);
  const [list, setList] = useState<Note[]>([]);
  const [page, setPage] = useState(1);

  useEffect(() => { refresh(); }, [page]);

  async function refresh() {
    const rows = await invoke<Note[]>("list_notes", { page, pageSize: 10 });
    setList(rows);
  }

  async function onSave() {
    if (!title.trim() || !content.trim()) return;
    setSaving(true);
    try {
      if (editingId == null) {
        await invoke<number>("add_note", { title, content });
      } else {
        await invoke("update_note", { id: editingId, title, content });
        setEditingId(null);
      }
      setTitle("");
      setContent("");
      await refresh();
      alert("已保存！");
    } catch (e) {
      console.error(e);
      alert("保存失败: " + e);
    } finally {
      setSaving(false);
    }
  }

  async function onEdit(n: Note) {
    setEditingId(n.id);
    setTitle(n.title);
    setContent(n.content);
  }

  async function onDelete(id: number) {
    if (!confirm("确认删除？")) return;
    await invoke("delete_note", { id });
    await refresh();
  }

  async function onSearch() {
    try {
      const q2 = q.trim();
      if (!q2) { setResults([]); return; }
      const rows = await invoke<Hit[]>("search_notes", { query: q2 });
      setResults(rows);
    } catch (e) {
      console.error(e);
      alert("搜索失败: " + e);
    }
  }

  const renderSnippet = (s: string) =>
    ({ __html: s.replaceAll("[mark]", "<mark>").replaceAll("[/mark]", "</mark>") });

  // ✅ 移到组件内部
  const onExport = async () => {
    try {
      const res = await invoke<{ path: string; count: number }>("export_notes_jsonl");
      alert(`已导出 ${res.count} 条到：\n${res.path}`);
    } catch (e: any) {
      alert("导出失败: " + e);
    }
  };

  // ✅ 移到组件内部，并在成功后调用 refresh()
  async function onImport() {
    try {
      const res = await invoke<[number, number]>("import_notes_jsonl");
      const [inserted, ignored] = res;
      alert(`已导入：${inserted} 条；忽略：${ignored} 条（已存在的重复）`);
      await refresh();
    } catch (e: any) {
      alert("导入失败: " + e);
    }
  }

  // 放在组件里其它 state 后面
  type PlanTask = {
    id: number;
    skill_id?: number | null;
    title: string;
    minutes: number;
    due?: string | null;
    status: string;
    horizon: string;
  };

  const [plans, setPlans] = useState<PlanTask[]>([]);
  const [planHorizon, setPlanHorizon] = useState<"WEEK" | "QTR">("WEEK");
  const [planFilter, setPlanFilter] = useState<"ALL" | "TODO" | "DONE">("ALL");

  async function loadPlans(h?: "WEEK" | "QTR", s?: "ALL" | "TODO" | "DONE") {
    const horizon = h ?? planHorizon;
    const status = (s ?? planFilter) === "ALL" ? null : (s ?? planFilter);
    const rows = await invoke<PlanTask[]>("list_plan_tasks", {
      horizon, status
    });
    setPlans(rows);
  }

  async function onSeedIndustry() {
    const n = await invoke<number>("seed_industry_v1");
    alert(`行业树种子写入：${n} 项`);
  }

  async function onGenerate(h: "WEEK" | "QTR") {
    await invoke<PlanTask[]>("generate_plan", { horizon: h });
    await loadPlans(h, planFilter);
    alert(h === "WEEK" ? "已生成周计划" : "已生成三月计划");
  }

  async function toggleDone(t: PlanTask) {
    const next = t.status === "DONE" ? "TODO" : "DONE";
    await invoke("update_plan_status", { id: t.id, status: next });
    await loadPlans();
  }

  useEffect(() => { loadPlans(); }, []);

  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <h2>{hello(name)}</h2>
      <input
        value={name} onChange={(e) => setName(e.target.value)}
        placeholder="Enter a name..." style={{ padding: 8, borderRadius: 8, marginBottom: 16 }}
      />

      <hr style={{ margin: "16px 0" }} />

      <h3>{editingId ? "编辑记录" : "新增记录"}</h3>
      <input
        value={title} onChange={(e) => setTitle(e.target.value)} placeholder="标题"
        style={{ width: "100%", padding: 8, borderRadius: 8, marginBottom: 8 }}
      />
      <textarea
        value={content} onChange={(e) => setContent(e.target.value)} placeholder="正文内容..."
        rows={4} style={{ width: "100%", padding: 8, borderRadius: 8 }}
      />
      <div style={{ marginTop: 8, display: "flex", gap: 8 }}>
        <button onClick={onSave} disabled={saving} style={{ padding: "8px 12px" }}>
          {saving ? "保存中..." : (editingId ? "更新" : "保存")}
        </button>
        {editingId && (
          <button onClick={() => { setEditingId(null); setTitle(""); setContent(""); }}>
            取消编辑
          </button>
        )}
      </div>

      <h3 style={{ marginTop: 24 }}>全文搜索（FTS5）</h3>
      <input
        value={q} onChange={(e) => setQ(e.target.value)} placeholder='关键字 / "短语" / a OR b'
        style={{ padding: 8, borderRadius: 8, width: "100%" }}
      />
      <div style={{ marginTop: 8 }}>
        <button onClick={onSearch} style={{ padding: "8px 12px" }}>搜索</button>
      </div>
      <ul style={{ marginTop: 16, lineHeight: 1.6 }}>
        {results.map((hit) => (
          <li key={hit.id} style={{ marginBottom: 12 }}>
            <div style={{ fontWeight: 600 }}>{hit.title}</div>
            <div dangerouslySetInnerHTML={renderSnippet(hit.snippet)} />
          </li>
        ))}
      </ul>

      <h3 style={{ marginTop: 24 }}>最近记录</h3>
      <div style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 16 }}>
        <h3 style={{ margin: 0 }}>最近记录</h3>
        <button onClick={onExport}>导出 JSONL</button>
        <button onClick={onImport}>导入 JSONL</button>
      </div>

      <ul style={{ marginTop: 8 }}>
        {list.map(n => (
          <li key={n.id} style={{ marginBottom: 10 }}>
            <div style={{ fontWeight: 600 }}>{n.title}</div>
            <div style={{ fontSize: 12, opacity: 0.7 }}>{n.created_at}</div>
            <div style={{ marginTop: 6, display: "flex", gap: 8 }}>
              <button onClick={() => onEdit(n)}>编辑</button>
              <button onClick={() => onDelete(n.id)}>删除</button>
            </div>
          </li>
        ))}
      </ul>
      <div style={{ display: "flex", gap: 8 }}>
        <button disabled={page === 1} onClick={() => setPage(p => Math.max(1, p - 1))}>上一页</button>
        <button onClick={() => setPage(p => p + 1)}>下一页</button>
        <button onClick={refresh}>刷新</button>
      </div>

      {/* ---- 计划区块（新增） ---- */}
      <h3 style={{ marginTop: 24 }}>计划</h3>
      <div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 8 }}>
        <button onClick={onSeedIndustry}>初始化行业树种子</button>
        <button onClick={() => onGenerate("WEEK")}>生成周计划</button>
        <button onClick={() => onGenerate("QTR")}>生成三月计划</button>

        <select
          value={planHorizon}
          onChange={(e) => { const v = e.target.value as "WEEK" | "QTR"; setPlanHorizon(v); loadPlans(v, planFilter); }}
          style={{ marginLeft: 12 }}
        >
          <option value="WEEK">WEEK</option>
          <option value="QTR">QTR</option>
        </select>

        <select
          value={planFilter}
          onChange={(e) => { const v = e.target.value as "ALL"|"TODO"|"DONE"; setPlanFilter(v); loadPlans(planHorizon, v); }}
        >
          <option value="ALL">全部</option>
          <option value="TODO">TODO</option>
          <option value="DONE">DONE</option>
        </select>

        <button onClick={() => loadPlans()}>刷新</button>
      </div>

      <ul style={{ marginTop: 8 }}>
        {plans.map(t => (
          <li key={t.id} style={{ marginBottom: 10, display: "flex", gap: 8, alignItems: "center" }}>
            <input type="checkbox" checked={t.status === "DONE"} onChange={() => toggleDone(t)} />
            <div style={{ flex: 1 }}>
              <div style={{ fontWeight: 600 }}>{t.title}</div>
              <div style={{ fontSize: 12, opacity: 0.7 }}>
                {t.horizon} · {t.minutes} 分钟{t.due ? ` · 截止 ${t.due}` : ""}
              </div>
            </div>
            <span style={{
              fontSize: 12,
              padding: "2px 6px",
              borderRadius: 6,
              background: t.status === "DONE" ? "#e6ffed" : "#fffbe6",
              border: "1px solid #ddd"
            }}>
              {t.status}
            </span>
          </li>
        ))}
      </ul>
    </div>
  );
}


第 3 步｜运行与自检（一步一步）
路径与你当前 main.rs 的数据目录一致：C:\Users\wwlie\AppData\Roaming\KnoYoo\Desktop\data\notes.db。
# A. 重新编译后端（确保命令已导出）
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

# B. 启动开发模式
cd D:\knoyoo\apps\desktop
pnpm tauri dev

在应用窗口中：
1. 点击【初始化行业树种子】 → 弹窗提示“写入 N 项”。
2. 点击【生成周计划】或【生成三月计划】 → 列表出现任务（TODO）。
3. 勾选复选框 → 该任务 status 变为 DONE，再点一次恢复 TODO。
4. 切换 Horizon（WEEK/QTR）、筛选（全部/TODO/DONE），查看列表变化。

一切正常。

------------------------------------
你现在所在的里程碑
✅ 记录/检索最小闭环（C 完成）
✅ 计划引擎落库 + 前端接入（本步完成）
↗️ 下一个小目标（仍在 v1.0 范围内）：
1. 在保存笔记时调用 classify_and_update(id)（自动归类、提升 mastery）；
2. 做一个“成长树 v1”最小可视化（先用简单列表或树组件占位）；
3. 周报简版：统计 TODO→DONE 数与 mastery 变化（可直接查询两张表生成）。
------------------------------------

可选优化（保持 v1.0 简洁）
1. 任务 due：目前为 NULL。后面做“日历排期/自动分散”再填充（PRD 里 v1.1/之后）。
2. classify_and_update：等前端把“笔记保存后→自动归类+提升 mastery”串起来，generate_plan 的排序会更贴近真实差距（Sprint 3–4 内容）。

需要的话，我可以再给你一版「保存笔记后自动归类」的前端改动（只改两行），以及“周报简版”的最小后端接口与 UI。
我需要。

我们把 3 件事一次性做好并可自检：
A) 任务 due 自动排期（周/三月分散）；
B) “保存笔记→自动归类+提升 mastery”；
C) “周报简版”最小版（任务完成数/总时长、近 7 天新增笔记数、当前短板 Top5）。
这三项都对齐你文档里的 v1.0/1.1 路线（固定周期计划、周报简版、后续做日历排期加强）。


A. 后端：计划 due 自动排期（分散到周/三月）

1) 在 Cargo.toml 加依赖
文件：D:\knoyoo\apps\desktop\src-tauri\Cargo.toml（[dependencies] 下追加）
chrono = { version = "0.4", features = ["serde", "clock"] }

2) 改 generate_plan：按周期分散 due
文件：D:\knoyoo\apps\desktop\src-tauri\src\main.rs
在文件顶部 use 区补一行：
use chrono::{Local, Duration};

用下面版本覆盖我当前的 generate_plan（仅此函数体替换，其他不动）：
#[tauri::command]
fn generate_plan(horizon: String) -> Result<Vec<PlanTaskOut>, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    let mut out = Vec::new();

    // 计算分散排期：WEEK → 7 天；QTR → 12 周（约三月）
    let now = Local::now().date_naive();
    let (slots, step_days) = match horizon.as_str() {
        "QTR" => (12, 7),  // 12 个周槽位
        _     => (7, 1),   // 默认 WEEK：7 天
    };
    let mut assigned = 0usize;

    {
        let mut stmt = tx.prepare(
            "SELECT s.id, s.name, s.importance, COALESCE(g.mastery,0) as mastery, s.level
               FROM industry_skill s
               LEFT JOIN growth_node g ON s.id = g.skill_id
              ORDER BY (s.level - COALESCE(g.mastery,0)) * s.importance DESC, s.importance DESC
              LIMIT 5"
        ).map_err(|e| e.to_string())?;

        let mut rows = stmt.query([]).map_err(|e| e.to_string())?;

        while let Some(row) = rows.next().map_err(|e| e.to_string())? {
            let skill_id: i64 = row.get(0).map_err(|e| e.to_string())?;
            let name: String = row.get(1).map_err(|e| e.to_string())?;
            let importance: i64 = row.get(2).map_err(|e| e.to_string())?;
            let mastery: i64 = row.get(3).map_err(|e| e.to_string())?;
            let level: i64 = row.get(4).map_err(|e| e.to_string())?;
            let gap = level - mastery;
            if gap <= 0 { continue; }

            let minutes = 60 * importance.min(gap);
            let title = format!("提升{}（差距{}）", name, gap);

            // 分配 due（均匀分散）
            let slot_idx = assigned % slots;
            let due_date = now + Duration::days((slot_idx * step_days) as i64);
            let due_str = due_date.format("%Y-%m-%d").to_string();
            assigned += 1;

            tx.execute(
                "INSERT INTO plan_task (horizon, skill_id, title, minutes, due, status)
                 VALUES (?1, ?2, ?3, ?4, ?5, 'TODO')",
                rusqlite::params![&horizon, skill_id, &title, minutes, &due_str],
            ).map_err(|e| e.to_string())?;

            let id = tx.last_insert_rowid();
            out.push(PlanTaskOut {
                id,
                skill_id: Some(skill_id),
                title,
                minutes,
                due: Some(due_str),
                status: "TODO".to_string(),
            });
        }
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok(out)
}

说明：
1. WEEK：任务均匀落到接下来 7 天；QTR：均匀落到接下来 12 个“周槽位”。
2. 这是 v1.0 的“最小排期”，后续我们再做“避免超分配、按空闲时段自动分散”的 v1.1 强化。



B. 后端：周报简版（最小版接口）
1) 新增接口 report_week_summary
仍在 main.rs 新增（放在 list_plan_tasks 附近即可）：
#[derive(Serialize)]
struct WeekReport {
    start: String,
    end: String,
    tasks_done: i64,
    minutes_done: i64,
    new_notes: i64,
    avg_mastery: f64,
    top_gaps: Vec<(String, i64, i64, i64)>, // name, required_level, mastery, gap
}

#[tauri::command]
fn report_week_summary() -> Result<WeekReport, String> {
    use chrono::Datelike;
    let conn = open_db()?;

    // 本周：以“今天往前 6 天”作为一周窗口
    let today = Local::now().date_naive();
    let start = today - Duration::days(6);
    let start_s = start.format("%Y-%m-%d").to_string();
    let end_s   = today.format("%Y-%m-%d").to_string();

    // 1) 本周完成的任务（按 due 统计）
    let mut stmt = conn.prepare(
        "SELECT COUNT(*), COALESCE(SUM(minutes),0)
           FROM plan_task
          WHERE status='DONE' AND due IS NOT NULL
            AND due >= ?1 AND due <= ?2"
    ).map_err(|e| e.to_string())?;
    let (tasks_done, minutes_done):(i64,i64) = stmt.query_row(rusqlite::params![&start_s, &end_s], |r| Ok((r.get(0)?, r.get(1)?))).map_err(|e| e.to_string())?;

    // 2) 近 7 天新增笔记
    let mut stmt2 = conn.prepare(
        "SELECT COUNT(*) FROM notes WHERE datetime(created_at) >= datetime(?1 || 'T00:00:00Z')"
    ).map_err(|e| e.to_string())?;
    let new_notes: i64 = stmt2.query_row([&start_s], |r| r.get(0)).map_err(|e| e.to_string())?;

    // 3) 当前平均 mastery
    let mut stmt3 = conn.prepare("SELECT COALESCE(AVG(mastery),0.0) FROM growth_node")
        .map_err(|e| e.to_string())?;
    let avg_mastery: f64 = stmt3.query_row([], |r| r.get(0)).map_err(|e| e.to_string())?;

    // 4) 当前短板 Top5（gap*importance）
    let mut stmt4 = conn.prepare(
        "SELECT s.name, s.level AS required_level, COALESCE(g.mastery,0) as mastery,
                (s.level-COALESCE(g.mastery,0)) AS gap
           FROM industry_skill s
           LEFT JOIN growth_node g ON s.id = g.skill_id
          ORDER BY (s.level-COALESCE(g.mastery,0))*s.importance DESC, s.importance DESC
          LIMIT 5"
    ).map_err(|e| e.to_string())?;
    let rows = stmt4.query_map([], |row| {
        Ok((
            row.get::<_, String>(0)?,
            row.get::<_, i64>(1)?,
            row.get::<_, i64>(2)?,
            row.get::<_, i64>(3)?,
        ))
    }).map_err(|e| e.to_string())?;
    let mut top_gaps = Vec::new();
    for r in rows { top_gaps.push(r.map_err(|e| e.to_string())?); }

    Ok(WeekReport{ start: start_s, end: end_s, tasks_done, minutes_done, new_notes, avg_mastery, top_gaps })
}

2) 把新命令导出

找到 invoke_handler!，追加：
  , report_week_summary


C. 前端改动：
1. “保存笔记→自动归类”【只改两行】
2. 新增“周报简版”区块
3. 计划列表已显示 due，不需改动

1) 保存后自动归类（只改我的保存逻辑）
文件：D:\knoyoo\apps\desktop\src\App.tsx
在你当前的“保存记录”方法里，把：
await invoke<number>("add_note", { title, content });
改成：
const id = await invoke<number>("add_note", { title, content });
// 保存成功后，立刻做“自动归类 + mastery 提升”
await invoke("classify_and_update", { noteId: id });
这样就把“笔记 → 行业树映射 → mastery +10”的链路串起来了；生成计划的排序会更贴近真实差距（Sprint 3–4）。

2) 周报简版 UI
在 App.tsx（我放“计划”区块的附近）新增一个“周报”区块：
type WeekReport = {
  start: string; end: string;
  tasks_done: number; minutes_done: number;
  new_notes: number; avg_mastery: number;
  top_gaps: [string, number, number, number][];
};

const [report, setReport] = useState<WeekReport | null>(null);

async function loadWeekReport() {
  const r = await invoke<WeekReport>("report_week_summary");
  setReport(r);
}

// 在 JSX 里加一个模块（放哪都行）
<h3 style={{ marginTop: 24 }}>周报简版</h3>
<div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 8 }}>
  <button onClick={loadWeekReport}>生成本周简报</button>
</div>

{report && (
  <div style={{ lineHeight: 1.6 }}>
    <div>统计范围：{report.start} ~ {report.end}</div>
    <div>完成任务：{report.tasks_done} 个（约 {report.minutes_done} 分钟）</div>
    <div>新增笔记：{report.new_notes} 条</div>
    <div>当前平均掌握度：{report.avg_mastery.toFixed(1)}</div>
    <div style={{ marginTop: 8, fontWeight: 600 }}>短板 Top5：</div>
    <ol>
      {report.top_gaps.map(([name, req, m, gap], i) => (
        <li key={i}>{name}：要求 {req}，当前 {m}，差距 {gap}</li>
      ))}
    </ol>
  </div>
)}


D. 自检步骤（一步一步）
# 1) 重新编译后端（确保 chrono + 新命令生效）
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

# 2) 启动应用
cd D:\knoyoo\apps\desktop
pnpm tauri dev

1. 在“新增记录”保存 1–2 条后，检查控制台是否无报错（保存后后台会自动 classify_and_update）。
2. 点击【初始化行业树种子】（若还没初始化）→ 再点【生成周计划/三月计划】 → 计划列表应带 due（WEEK=未来 7 天；QTR=未来 12 周）。
3. 勾选几条任务为 DONE，点【生成本周简报】→ 能看到“完成任务个数/总分钟、近 7 天新增笔记数、当前平均 mastery、短板 Top5”。
（由于我们尚未记录 mastery 历史，周报里的“能力变化”先以“当前平均 mastery + 短板 Top5”来呈现，后续在 v1.1/之后用日志或快照做“变化量”。这与文档里“周报简版、后续增强报告/排期”一致。 ）


发现问题： title：机器学习： content：SVM学会 
搜索时候写SVM搜索不出来。 

这是个经典边界：英文词紧贴中文（SVM学会）在 FTS5 的 unicode61 分词器里会被当成一个连续 token（形如 svm学会），所以用 MATCH 'SVM' 匹配不到。解决思路很简单：
先用 FTS5 搜；若结果为空且查询是纯 ASCII 单词（比如 SVM、NLP），自动回退一轮 LIKE 模糊匹配，这样就能命中像 SVM学会 这种“中英相连”的文本了。

下面给精准改法（只改 search_notes 函数的实现），以及自检步骤。改完后我们继续下一步计划。

该改哪：src-tauri/src/main.rs → search_notes（替换此函数体）
把我现有的 search_notes 函数整体替换为下面版本（其余文件不动）：
#[tauri::command]
fn search_notes(query: String) -> Result<Vec<Hit>, String> {
    let conn = open_db()?;

    // 判定：中文 / 含数字或符号 / 超短词 → 直接 LIKE
    let need_like_primary = has_cjk(&query) || has_digit_or_symbol(&query) || query.chars().count() <= 2;

    // 小工具：是否是“纯 ASCII 英数单词”（例如 SVM、NLP、AI，不含空格）
    fn is_ascii_alnum_word(s: &str) -> bool {
        !s.is_empty() && s.chars().all(|c| c.is_ascii_alphanumeric())
    }

    // LIKE 路径（标题/正文都搜），带简单片段
    let like_search = |conn: &Connection, q: &str| -> Result<Vec<Hit>, String> {
        let like = format!("%{}%", q);
        let mut stmt = conn.prepare(
            "SELECT id, title, content
               FROM notes
              WHERE title LIKE ?1 OR content LIKE ?1
              ORDER BY datetime(created_at) DESC
              LIMIT 50",
        ).map_err(|e| e.to_string())?;

        let rows = stmt.query_map([like], |row| {
            let id: i64 = row.get(0)?;
            let title: String = row.get(1)?;
            let content: String = row.get(2)?;
            let snippet = make_snippet(&content, q, 24);
            Ok(Hit { id, title, snippet })
        }).map_err(|e| e.to_string())?;

        let mut out = Vec::new();
        for r in rows { out.push(r.map_err(|e| e.to_string())?); }
        Ok(out)
    };

    if need_like_primary {
        // 直接 LIKE
        return like_search(&conn, &query);
    }

    // 首选 FTS5（英文/可分词）
    let mut stmt = conn.prepare(
        "SELECT n.id, n.title,
                COALESCE(NULLIF(snippet(notes_fts, 1, '[mark]', '[/mark]', ' … ', 12), ''),
                         snippet(notes_fts, 0, '[mark]', '[/mark]', ' … ', 12)) AS snip
           FROM notes_fts
           JOIN notes n ON n.id = notes_fts.rowid
          WHERE notes_fts MATCH ?1
          ORDER BY bm25(notes_fts) ASC
          LIMIT 50",
    ).map_err(|e| e.to_string())?;

    let rows = stmt.query_map([&query], |row| {
        Ok(Hit { id: row.get(0)?, title: row.get(1)?, snippet: row.get(2)? })
    }).map_err(|e| e.to_string())?;

    let mut out = Vec::new();
    for r in rows { out.push(r.map_err(|e| e.to_string())?); }

    // ⭐ 关键补救：若 FTS5 空且查询是纯 ASCII 单词（如 SVM），再回退 LIKE
    if out.is_empty() && is_ascii_alnum_word(&query) {
        return like_search(&conn, &query);
    }

    Ok(out)
}


原理：
1. SVM学会 在 FTS5 里是连续 token，MATCH 'SVM' 命不中；
2. 回退 LIKE '%SVM%' 能命中，并用 make_snippet 做高亮；
3. 对中文或带符号/短词，仍直接用 LIKE（你之前的策略保留）。
4. 英文长词（如 transformer）则优先走 FTS5，排序和高亮更好。


自检：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

cd D:\knoyoo\apps\desktop
pnpm tauri dev

问题解决了。

继续按计划推进：
我刚才说的三项优化我们前面已经给了完整实现与 UI 接入（自动 due 分散、保存后自动归类、周报简版）并配好自检。它们与版本节奏里“固定周期计划（周/三月）”“周报简版”“AI 分类更新成长树”完全对齐。

------------------------------------------------------------
下一步建议（按 v1.0 路线继续）
1. 保存笔记时将“命中的技能”在前端 toast 中提示（增强可见性）；
2. 做一个“成长树 v1 可视化（占位版）”：先把 industry_skill + growth_node 拉出来，按 parent/child 渲染成可展开的树；
3. 在“计划”区加按 due/minutes 的排序与筛选（小改 UI）。

很好，我目前的代码和功能已经完成了 录入、检索、归类、计划生成，并且通过了自检，这正对应 Sprint 2（Wk3–Wk4）到 Sprint 4（Wk7–Wk8） 的核心交付。下一步需要把我们讨论过的优化和计划内的功能一并落地

✅ 已完成 (你现在的状态)
1. 文本录入、检索（FTS + LIKE fallback）
2. AI 分类（关键词规则） → 成长树/行业树更新
3. 计划生成 (周/三月) 并落库
4. Bug 修复（英文/短词搜索、FTS snippet、SVM case）
----------------------------------------------------

1) 后端依赖

文件：D:\knoyoo\apps\desktop\src-tauri\Cargo.toml
在 [dependencies] 里新增一行（如果已存在就跳过）：
chrono = { version = "0.4", features = ["serde", "clock"] }

2) 后端 main.rs 改动
文件：D:\knoyoo\apps\desktop\src-tauri\src\main.rs

2.1 在顶部 use 区新增一行
在我的 use 们下面加入（若已加过，跳过）：
use chrono::{Local, Duration};

2.2 用“自动排期版”覆盖 generate_plan 函数体
找到我现有的：
#[tauri::command]
fn generate_plan(horizon: String) -> Result<Vec<PlanTaskOut>, String> {
    ...
}
把整个函数替换为下面这一版（均匀分散 due 到 7 天/12 周）：
#[tauri::command]
fn generate_plan(horizon: String) -> Result<Vec<PlanTaskOut>, String> {
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    let mut out = Vec::new();

    // WEEK → 7 天；QTR → 12 周
    let today = Local::now().date_naive();
    let (slots, step_days) = match horizon.as_str() {
        "QTR" => (12, 7),
        _     => (7, 1),
    };
    let mut assigned = 0usize;

    {
        let mut stmt = tx.prepare(
            "SELECT s.id, s.name, s.importance, COALESCE(g.mastery,0) as mastery, s.level
               FROM industry_skill s
               LEFT JOIN growth_node g ON s.id = g.skill_id
              ORDER BY (s.level - COALESCE(g.mastery,0)) * s.importance DESC, s.importance DESC
              LIMIT 5"
        ).map_err(|e| e.to_string())?;

        let mut rows = stmt.query([]).map_err(|e| e.to_string())?;

        while let Some(row) = rows.next().map_err(|e| e.to_string())? {
            let skill_id: i64 = row.get(0).map_err(|e| e.to_string())?;
            let name: String = row.get(1).map_err(|e| e.to_string())?;
            let importance: i64 = row.get(2).map_err(|e| e.to_string())?;
            let mastery: i64 = row.get(3).map_err(|e| e.to_string())?;
            let level: i64 = row.get(4).map_err(|e| e.to_string())?;
            let gap = level - mastery;
            if gap <= 0 { continue; }

            let minutes = 60 * importance.min(gap);
            let title = format!("提升{}（差距{}）", name, gap);

            // 分散 due
            let slot = assigned % slots;
            let due = today + Duration::days((slot * step_days) as i64);
            let due_str = due.format("%Y-%m-%d").to_string();
            assigned += 1;

            tx.execute(
                "INSERT INTO plan_task (horizon, skill_id, title, minutes, due, status)
                 VALUES (?1, ?2, ?3, ?4, ?5, 'TODO')",
                rusqlite::params![&horizon, skill_id, &title, minutes, &due_str],
            ).map_err(|e| e.to_string())?;

            let id = tx.last_insert_rowid();
            out.push(PlanTaskOut {
                id,
                skill_id: Some(skill_id),
                title,
                minutes,
                due: Some(due_str),
                status: "TODO".to_string(),
            });
        }
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok(out)
}

2.3 新增「周报简版」接口
把下面整段粘贴到 main.rs 中（放在 list_plan_tasks 附近或文件末尾都可以）：
#[derive(Serialize)]
struct WeekReport {
    start: String,
    end: String,
    tasks_done: i64,
    minutes_done: i64,
    new_notes: i64,
    avg_mastery: f64,
    top_gaps: Vec<(String, i64, i64, i64)>, // name, required_level, mastery, gap
}

#[tauri::command]
fn report_week_summary() -> Result<WeekReport, String> {
    let conn = open_db()?;

    // 本周窗口：今天往前 6 天
    let end = Local::now().date_naive();
    let start = end - Duration::days(6);
    let start_s = start.format("%Y-%m-%d").to_string();
    let end_s   = end.format("%Y-%m-%d").to_string();

    // 1) 本周完成任务数 / 总分钟
    let (tasks_done, minutes_done):(i64,i64) = conn.query_row(
        "SELECT COUNT(*), COALESCE(SUM(minutes),0)
           FROM plan_task
          WHERE status='DONE' AND due IS NOT NULL
            AND due >= ?1 AND due <= ?2",
        rusqlite::params![&start_s, &end_s],
        |r| Ok((r.get(0)?, r.get(1)?))
    ).unwrap_or((0,0));

    // 2) 近 7 天新增笔记
    let new_notes: i64 = conn.query_row(
        "SELECT COUNT(*) FROM notes WHERE datetime(created_at) >= datetime(?1 || 'T00:00:00Z')",
        [&start_s],
        |r| r.get(0)
    ).unwrap_or(0);

    // 3) 当前平均 mastery
    let avg_mastery: f64 = conn.query_row(
        "SELECT COALESCE(AVG(mastery),0.0) FROM growth_node",
        [],
        |r| r.get(0)
    ).unwrap_or(0.0);

    // 4) 短板 Top5（gap*importance）
    let mut stmt4 = conn.prepare(
        "SELECT s.name, s.level AS required_level, COALESCE(g.mastery,0) as mastery,
                (s.level-COALESCE(g.mastery,0)) AS gap
           FROM industry_skill s
           LEFT JOIN growth_node g ON s.id = g.skill_id
          ORDER BY (s.level-COALESCE(g.mastery,0))*s.importance DESC, s.importance DESC
          LIMIT 5"
    ).map_err(|e| e.to_string())?;
    let rows = stmt4.query_map([], |row| {
        Ok((
            row.get::<_, String>(0)?,
            row.get::<_, i64>(1)?,
            row.get::<_, i64>(2)?,
            row.get::<_, i64>(3)?,
        ))
    }).map_err(|e| e.to_string())?;
    let mut top_gaps = Vec::new();
    for r in rows { top_gaps.push(r.map_err(|e| e.to_string())?); }

    Ok(WeekReport{ start: start_s, end: end_s, tasks_done, minutes_done, new_notes, avg_mastery, top_gaps })
}

2.4 把新命令导出
, report_week_summary
提醒：我之前已经有 list_plan_tasks / update_plan_status；如果命令列表里没它们，也一并加上。


3) 前端 App.tsx 改动
文件：D:\knoyoo\apps\desktop\src\App.tsx

3.1 保存后自动归类（只改两行）
找到你保存笔记的代码（通常在“保存”按钮点击里），把这行：
await invoke<number>("add_note", { title, content });
改成这两行（新增第二行）：
const id = await invoke<number>("add_note", { title, content });
// 保存成功后自动归类 + 提升 mastery
await invoke("classify_and_update", { noteId: id });
可选：显示一个提示
// 可选提示
// alert("已自动归类并更新掌握度");


3.2 计划列表里显示 due（如果你已有，就不用改）
我之前的“计划”区块如果还没显示 due，给任务行里加上：
<div style={{ fontSize: 12, opacity: 0.7 }}>
  {t.horizon} · {t.minutes} 分钟{t.due ? ` · 截止 ${t.due}` : ""}
</div>

3.3 新增“周报简版”区块
在文件顶部已有的 import 附近确保：
import { invoke } from "@tauri-apps/api/core";
import { useState, useEffect } from "react";
在组件中新增状态与加载函数：
type WeekReport = {
  start: string; end: string;
  tasks_done: number; minutes_done: number;
  new_notes: number; avg_mastery: number;
  top_gaps: [string, number, number, number][];
};
const [report, setReport] = useState<WeekReport | null>(null);
async function loadWeekReport() {
  const r = await invoke<WeekReport>("report_week_summary");
  setReport(r);
}

在 JSX（比如“计划”模块下面）插入以下区块：
<h3 style={{ marginTop: 24 }}>周报简版</h3>
<div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 8 }}>
  <button onClick={loadWeekReport}>生成本周简报</button>
</div>
{report && (
  <div style={{ lineHeight: 1.6 }}>
    <div>统计范围：{report.start} ~ {report.end}</div>
    <div>完成任务：{report.tasks_done} 个（约 {report.minutes_done} 分钟）</div>
    <div>新增笔记：{report.new_notes} 条</div>
    <div>当前平均掌握度：{report.avg_mastery.toFixed(1)}</div>
    <div style={{ marginTop: 8, fontWeight: 600 }}>短板 Top5：</div>
    <ol>
      {report.top_gaps.map(([name, req, m, gap], i) => (
        <li key={i}>{name}：要求 {req}，当前 {m}，差距 {gap}</li>
      ))}
    </ol>
  </div>
)}

4) 自检（PowerShell）
# 重新编译后端
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

# 运行
cd D:\knoyoo\apps\desktop
pnpm tauri dev

首先代码能跑起来，但是我有一些困惑和不满意：
1. 首先我不知道为什么有这么多重复的计划如，然后这些计划也不能做更改和删除。 
2. 我新增了一条AI方面的笔记，我并没有看到任何归类的表现。意思是 我没有看到软件里有对这条笔记进行归类。

现在解决问题：

一、问题 1：计划重复、且不能更改/删除
目标
1. 生成计划时避免重复（同一 horizon + skill_id 的未完成任务，仅保留一条最近的）。
2. 前端支持编辑/删除计划任务。
后端改动（src-tauri/src/main.rs）
1) 给 generate_plan 增加“去重”逻辑
位置：替换我当前的 generate_plan 函数体（其它不动）
#[tauri::command]
fn generate_plan(horizon: String) -> Result<Vec<PlanTaskOut>, String> {
    use chrono::{Local, Duration};
    let mut conn = open_db()?;
    let tx = conn.transaction().map_err(|e| e.to_string())?;

    let mut out = Vec::new();

    // WEEK → 7 天；QTR → 12 周
    let today = Local::now().date_naive();
    let (slots, step_days) = match horizon.as_str() {
        "QTR" => (12, 7),
        _     => (7, 1),
    };
    let mut assigned = 0usize;

    {
        let mut stmt = tx.prepare(
            "SELECT s.id, s.name, s.importance, COALESCE(g.mastery,0) as mastery, s.level
               FROM industry_skill s
               LEFT JOIN growth_node g ON s.id = g.skill_id
              ORDER BY (s.level - COALESCE(g.mastery,0)) * s.importance DESC, s.importance DESC
              LIMIT 5"
        ).map_err(|e| e.to_string())?;

        let mut rows = stmt.query([]).map_err(|e| e.to_string())?;

        while let Some(row) = rows.next().map_err(|e| e.to_string())? {
            let skill_id: i64 = row.get(0).map_err(|e| e.to_string())?;
            let name: String = row.get(1).map_err(|e| e.to_string())?;
            let importance: i64 = row.get(2).map_err(|e| e.to_string())?;
            let mastery: i64 = row.get(3).map_err(|e| e.to_string())?;
            let level: i64 = row.get(4).map_err(|e| e.to_string())?;
            let gap = level - mastery;
            if gap <= 0 { continue; }

            // ⭐ 去重：若已存在同 horizon+skill 的未完成任务（且截止日在今天及以后），就跳过
            let exists: Option<i64> = tx.query_row(
                "SELECT id FROM plan_task
                  WHERE horizon=?1 AND skill_id=?2
                    AND status<>'DONE'
                    AND (due IS NULL OR due >= date('now'))
                  LIMIT 1",
                rusqlite::params![&horizon, skill_id],
                |r| r.get(0),
            ).optional().map_err(|e| e.to_string())?;
            if exists.is_some() {
                continue; // 已有未完成任务：不再重复插入
            }

            let minutes = 60 * importance.min(gap);
            let title = format!("提升{}（差距{}）", name, gap);

            // 分散 due
            let slot = assigned % slots;
            let due = today + Duration::days((slot * step_days) as i64);
            let due_str = due.format("%Y-%m-%d").to_string();
            assigned += 1;

            tx.execute(
                "INSERT INTO plan_task (horizon, skill_id, title, minutes, due, status)
                 VALUES (?1, ?2, ?3, ?4, ?5, 'TODO')",
                rusqlite::params![&horizon, skill_id, &title, minutes, &due_str],
            ).map_err(|e| e.to_string())?;

            let id = tx.last_insert_rowid();
            out.push(PlanTaskOut {
                id,
                skill_id: Some(skill_id),
                title,
                minutes,
                due: Some(due_str),
                status: "TODO".to_string(),
            });
        }
    }

    tx.commit().map_err(|e| e.to_string())?;
    Ok(out)
}
说明：这会阻止“重复生成”同一技能的未完成任务（你列表里的成排重复就是这个原因）。

2) 新增两个命令：删除 & 编辑计划任务
位置：在 list_plan_tasks / update_plan_status 附近新增以下代码，并把命令导出到 invoke_handler!。
#[tauri::command]
fn delete_plan_task(id: i64) -> Result<(), String> {
    let mut conn = open_db()?;
    conn.execute("DELETE FROM plan_task WHERE id=?1", rusqlite::params![id])
        .map_err(|e| e.to_string())?;
    Ok(())
}

#[tauri::command]
fn update_plan_task(id: i64, title: String, minutes: i64, due: Option<String>) -> Result<(), String> {
    let mut conn = open_db()?;
    conn.execute(
        "UPDATE plan_task SET title=?1, minutes=?2, due=?3 WHERE id=?4",
        rusqlite::params![title, minutes, due, id],
    ).map_err(|e| e.to_string())?;
    Ok(())
}
在 main() 的 .invoke_handler(tauri::generate_handler![ ... ]) 列表末尾追加：
, delete_plan_task, update_plan_task

前端改动（apps/desktop/src/App.tsx）
3) 在“计划”列表中加入【编辑】【删除】
位置：我渲染 plans.map(t => ...) 的 <li> 里，状态标签旁边追加两个按钮。
<button onClick={async () => {
  const title = window.prompt("新标题：", t.title) ?? t.title;
  const minutesStr = window.prompt("分钟数：", String(t.minutes)) ?? String(t.minutes);
  const due = window.prompt("截止日期(YYYY-MM-DD，可留空)：", t.due ?? "") ?? (t.due ?? "");
  const minutes = parseInt(minutesStr || "0", 10) || 0;
  await invoke("update_plan_task", { id: t.id, title, minutes, due: due || null });
  await loadPlans();
}}>编辑</button>

<button onClick={async () => {
  if (confirm("删除这条任务？")) {
    await invoke("delete_plan_task", { id: t.id });
    await loadPlans();
  }
}} style={{ marginLeft: 6 }}>删除</button>
现在我可以直接在 UI 中编辑标题/时长/截止日期，或删除任务。重复生成的问题也被后端拦住了



二、问题 2：新增 AI 笔记没有看到“归类”的表现
目标
1. 后端把归类结果（命中的技能、加分值、最新 mastery）返回给前端。
2. 前端保存笔记成功后弹出提示/Toast，直观看到“归类到了哪些技能”。

后端改动（src-tauri/src/main.rs）
1) 改造 classify_and_update 返回详细结果
位置：用下面版本覆盖你现有的 classify_and_update（签名会从 Result<u32> 变为 Result<Vec<...>>）

#[derive(Serialize)]
struct ClassifyHit {
    skill_id: i64,
    name: String,
    delta: i64,
    new_mastery: i64,
}

#[tauri::command]
fn classify_and_update(note_id: i64) -> Result<Vec<ClassifyHit>, String> {
    let conn = open_db()?;

    // 取笔记内容
    let mut stmt = conn.prepare("SELECT title, content FROM notes WHERE id=?1")
        .map_err(|e| e.to_string())?;
    let (title, content): (String, String) = stmt
        .query_row(rusqlite::params![note_id], |row| Ok((row.get(0)?, row.get(1)?)))
        .map_err(|e| e.to_string())?;
    let text = format!("{} {}", title, content);

    // 遍历技能
    let mut skill_stmt = conn.prepare("SELECT id, name FROM industry_skill")
        .map_err(|e| e.to_string())?;
    let skills = skill_stmt.query_map([], |row| Ok((row.get::<_, i64>(0)?, row.get::<_, String>(1)?)))
        .map_err(|e| e.to_string())?;

    let mut hits: Vec<ClassifyHit> = Vec::new();
    for s in skills {
        let (skill_id, name) = s.map_err(|e| e.to_string())?;
        if text.contains(&name) {
            // 写入映射表
            conn.execute(
                "INSERT OR IGNORE INTO note_skill_map (note_id, skill_id, weight) VALUES (?1, ?2, 1)",
                rusqlite::params![note_id, skill_id],
            ).map_err(|e| e.to_string())?;

            // mastery +10（如无则插入）
            let delta: i64 = 10;
            let updated = conn.execute(
                "UPDATE growth_node SET mastery = MIN(mastery+?2,100) WHERE skill_id=?1",
                rusqlite::params![skill_id, delta],
            ).map_err(|e| e.to_string())?;
            if updated == 0 {
                conn.execute(
                    "INSERT INTO growth_node (skill_id, mastery) VALUES (?1, ?2)",
                    rusqlite::params![skill_id, delta],
                ).map_err(|e| e.to_string())?;
            }

            // 取最新 mastery
            let new_mastery: i64 = conn.query_row(
                "SELECT mastery FROM growth_node WHERE skill_id=?1",
                [skill_id],
                |r| r.get(0),
            ).map_err(|e| e.to_string())?;

            hits.push(ClassifyHit { skill_id, name, delta, new_mastery });
        }
    }

    Ok(hits)
}
现在，归类会把“命中的技能名 + 增量 + 新掌握度”返回给前端。


前端改动（apps/desktop/src/App.tsx）
2) 保存后弹出“归类结果”提示
位置：我保存笔记的函数里，之前我们写了：
const id = await invoke<number>("add_note", { title, content });
await invoke("classify_and_update", { noteId: id });
把第二行改成下面这两行，把返回值展示出来：
const hits = await invoke<Array<{ skill_id: number; name: string; delta: number; new_mastery: number }>>(
  "classify_and_update",
  { noteId: id }
);
if (Array.isArray(hits) && hits.length > 0) {
  const msg = hits.map(h => `${h.name} +${h.delta} → ${h.new_mastery}`).join("，");
  alert(`已自动归类：${msg}`);
} else {
  alert("未命中任何技能（可增补行业树关键词或稍后用 AI 分类升级）");
}
这样我一保存笔记，就能看到“归类到了哪些技能”。
另外建议我输入的 AI 笔记里包含你种子技能的中文名（例如“机器学习”“数据分析”“大模型”等），因为 v1.0 的归类是规则法（按技能名匹配）。后续我们再把它升级成“关键词词表/Embedding/模型分类”。


三、自检步骤
# 后端重新编译
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

# 启动应用
cd D:\knoyoo\apps\desktop
pnpm tauri dev

powershell报错：
PS D:\knoyoo\apps\desktop> # 后端重新编译
PS D:\knoyoo\apps\desktop> cd D:\knoyoo\apps\desktop\src-tauri
PS D:\knoyoo\apps\desktop\src-tauri> cargo build
   Compiling desktop v0.1.0 (D:\knoyoo\apps\desktop\src-tauri)
error[E0599]: no method named `optional` found for enum `Result` in the current scope
   --> src\main.rs:556:15
    |
548 |               let exists: Option<i64> = tx.query_row(
    |  _______________________________________-
549 | |                 "SELECT id FROM plan_task
550 | |                   WHERE horizon=?1 AND skill_id=?2
551 | |                     AND status<>'DONE'
...   |
555 | |                 |r| r.get(0),
556 | |             ).optional().map_err(|e| e.to_string())?;
    | |              -^^^^^^^^ method not found in `Result<_, rusqlite::Error>`
    | |______________|
    |
    |
   ::: C:\Users\wwlie\.cargo\registry\src\index.crates.io-1949cf8c6b5b557f\rusqlite-0.31.0\src\lib.rs:279:8
    |
279 |       fn optional(self) -> Result<Option<T>>;
    |          -------- the method is available for `Result<_, rusqlite::Error>` here
    |
    = help: items from traits can only be used if the trait is in scope
help: trait `OptionalExtension` which provides `optional` is implemented but not in scope; perhaps you want to import it
    |
3   + use rusqlite::OptionalExtension;
    |

warning: variable does not need to be mutable
   --> src\main.rs:643:9
    |
643 |     let mut conn = open_db()?;
    |         ----^^^^
    |         |
    |         help: remove this `mut`
    |
    = note: `#[warn(unused_mut)]` on by default

warning: variable does not need to be mutable
   --> src\main.rs:653:9
    |
653 |     let mut conn = open_db()?;
    |         ----^^^^
    |         |
    |         help: remove this `mut`

warning: variable does not need to be mutable
   --> src\main.rs:661:9
    |
661 |     let mut conn = open_db()?;
    |         ----^^^^
    |         |
    |         help: remove this `mut`

For more information about this error, try `rustc --explain E0599`.
warning: `desktop` (bin "desktop") generated 3 warnings
error: could not compile `desktop` (bin "desktop") due to 1 previous error; 3 warnings emitted
PS D:\knoyoo\apps\desktop\src-tauri>
PS D:\knoyoo\apps\desktop\src-tauri> # 启动应用
PS D:\knoyoo\apps\desktop\src-tauri> cd D:\knoyoo\apps\desktop
PS D:\knoyoo\apps\desktop> pnpm tauri dev

> desktop@0.1.0 tauri D:\knoyoo\apps\desktop
> tauri "dev"

     Running BeforeDevCommand (`pnpm dev`)

> desktop@0.1.0 dev D:\KnoYoo\apps\desktop
> vite


  VITE v7.1.5  ready in 2304 ms

  ➜  Local:   http://localhost:1420/
     Running DevCommand (`cargo  run --no-default-features --color always --`)
        Info Watching D:\KnoYoo\apps\desktop\src-tauri for changes...
   Compiling desktop v0.1.0 (D:\KnoYoo\apps\desktop\src-tauri)
error[E0599]: no method named `optional` found for enum `Result` in the current scope
   --> src\main.rs:556:15
    |
548 |               let exists: Option<i64> = tx.query_row(
    |  _______________________________________-
549 | |                 "SELECT id FROM plan_task
550 | |                   WHERE horizon=?1 AND skill_id=?2
551 | |                     AND status<>'DONE'
...   |
555 | |                 |r| r.get(0),
556 | |             ).optional().map_err(|e| e.to_string())?;
    | |              -^^^^^^^^ method not found in `Result<_, rusqlite::Error>`
    | |______________|
    |
    |
   ::: C:\Users\wwlie\.cargo\registry\src\index.crates.io-1949cf8c6b5b557f\rusqlite-0.31.0\src\lib.rs:279:8
    |
279 |       fn optional(self) -> Result<Option<T>>;
    |          -------- the method is available for `Result<_, rusqlite::Error>` here
    |
    = help: items from traits can only be used if the trait is in scope
help: trait `OptionalExtension` which provides `optional` is implemented but not in scope; perhaps you want to import it
    |
3   + use rusqlite::OptionalExtension;
    |

warning: variable does not need to be mutable
   --> src\main.rs:643:9
    |
643 |     let mut conn = open_db()?;
    |         ----^^^^
    |         |
    |         help: remove this `mut`
    |
    = note: `#[warn(unused_mut)]` on by default

warning: variable does not need to be mutable
   --> src\main.rs:653:9
    |
653 |     let mut conn = open_db()?;
    |         ----^^^^
    |         |
    |         help: remove this `mut`

warning: variable does not need to be mutable
   --> src\main.rs:661:9
    |
661 |     let mut conn = open_db()?;
    |         ----^^^^
    |         |
    |         help: remove this `mut`

For more information about this error, try `rustc --explain E0599`.
warning: `desktop` (bin "desktop") generated 3 warnings
error: could not compile `desktop` (bin "desktop") due to 1 previous error; 3 warnings emitted

开始修复：
原因：
这是个一行导入导致的编译错误。
文件：D:\knoyoo\apps\desktop\src-tauri\src\main.rs
在我的 use 头部，把 rusqlite 的导入改成包含 OptionalExtension：
// 原来大概是：
use rusqlite::{params, Connection};
// 改成：
use rusqlite::{params, Connection, OptionalExtension};
说明：.optional() 是 rusqlite::OptionalExtension trait 提供的方法，不导入这个 trait，编译器就找不到该方法。

自检：
cd D:\knoyoo\apps\desktop\src-tauri
cargo build

cd D:\knoyoo\apps\desktop
pnpm tauri dev

一切正常


可选清理（只为把黄色 warning 去掉）已选择
报的几个 “variable does not need to be mutable” 警告，可以把这些函数里 let mut conn = open_db()?; 改成 let conn = open_db()?;：
1. delete_plan_task
2. update_plan_task
还有我新增的其他只读查询函数里如有 mut 也可以去掉
并且优化一些程序

做到：
1.从源头和数据层共同防重复
2.一键清理历史重复计划
3.提升查询性能的索引
4.消掉当前的 mut 警告（或一条命令自动修）

1) 后端：数据库层防重复 + 性能索引
文件：D:\knoyoo\apps\desktop\src-tauri\src\main.rs
在 open_db() 里的 conn.execute_batch(r#"... 那个大 SQL 串中，在末尾几行附近追加下面这些 DDL（不要删我已有的内容，只要把这几句插进去就行）：
-- —— 清理与约束（新增） ——

-- 1) growth_node：每个 skill 只保留一个节点
CREATE UNIQUE INDEX IF NOT EXISTS idx_growth_unique ON growth_node(skill_id);

-- 2) plan_task：只允许每个 (horizon, skill_id) 存在一条“未完成”任务（利用部分唯一索引）
CREATE UNIQUE INDEX IF NOT EXISTS idx_plan_open_unique
  ON plan_task(horizon, skill_id)
  WHERE status <> 'DONE';

-- 3) 查询性能小索引
CREATE INDEX IF NOT EXISTS idx_plan_hsd ON plan_task(horizon, status, due);
CREATE INDEX IF NOT EXISTS idx_note_skill_note ON note_skill_map(note_id);
CREATE INDEX IF NOT EXISTS idx_note_skill_skill ON note_skill_map(skill_id);

作用：
1. idx_plan_open_unique 直接从数据库层把“重复未完成任务”卡住（即使以后有人误改后端逻辑也进不去）。
2. 其它索引让计划/映射查询更稳更快。
3. growth_node 的唯一索引保证不会出现同一 skill 多行的异常数据。


2) 后端：一键清理历史重复计划
文件：src-tauri\src\main.rs
把下面新命令整段粘贴到 list_plan_tasks / update_plan_status 那一段旁边（放哪里都行，只要在 main.rs 里）：
#[tauri::command]
fn cleanup_plan_duplicates(horizon: Option<String>) -> Result<u32, String> {
    // 删除“未完成”的重复计划：对 (horizon, skill_id) 只保留 id 最大的那条（最近生成的）
    let conn = open_db()?;

    let (sql, params): (String, Vec<String>) = if let Some(h) = horizon {
        (
            "DELETE FROM plan_task
              WHERE status <> 'DONE'
                AND horizon = ?1
                AND id NOT IN (
                  SELECT MAX(id) FROM plan_task
                   WHERE status <> 'DONE' AND horizon = ?1
                   GROUP BY horizon, skill_id
                )".to_string(),
            vec![h],
        )
    } else {
        (
            "DELETE FROM plan_task
              WHERE status <> 'DONE'
                AND id NOT IN (
                  SELECT MAX(id) FROM plan_task
                   WHERE status <> 'DONE'
                   GROUP BY horizon, skill_id
                )".to_string(),
            vec![],
        )
    };

    let mut stmt = conn.prepare(&sql).map_err(|e| e.to_string())?;
    let changed = if params.is_empty() {
        stmt.execute([]).map_err(|e| e.to_string())?
    } else {
        stmt.execute(rusqlite::params![params[0]]).map_err(|e| e.to_string())?
    };
    Ok(changed as u32)
}
把命令导出：在 main() 的 invoke_handler(tauri::generate_handler![ ... ]) 列表里追加：
, cleanup_plan_duplicates


3) 前端：一键清理按钮
文件：D:\knoyoo\apps\desktop\src\App.tsx
在“计划”标题那排按钮的那一行（我已有：初始化/生成周/三月/筛选/刷新），再加一个按钮：
<button onClick={async () => {
  const n = await invoke<number>("cleanup_plan_duplicates", { horizon: planHorizon });
  alert(`已清理重复：${n} 条`);
  await loadPlans();
}}>清理重复</button>
这会只清理当前筛选的 WEEK 或 QTR。如果你想做“清理全部”，可以把 { horizon: planHorizon } 改成 {}，后端会清理所有周期的重复。

4) 小警告清理（任选其一）
方案 A：一条命令自动修（推荐） 已选择方法A
cd D:\knoyoo\apps\desktop\src-tauri
cargo fix --bin "desktop"

方案 B：手动把这几处改成不可变
把报黄的几个函数（你日志里 643、653、661 行）内的
改成
let conn = open_db()?;
（比如 delete_plan_task、update_plan_task、report_week_summary 等，只要没有修改 conn 就不要写 mut）

但是4A的方法powershell有error：
PS D:\knoyoo\apps\desktop\src-tauri> cargo fix --bin "desktop"
error: the working directory of this package has uncommitted changes, and `cargo fix` can potentially perform destructive changes; if you'd like to suppress this error pass `--allow-dirty`, or commit the changes to these files:

  * apps/desktop/package.json (dirty)
  * apps/desktop/src-tauri/Cargo.lock (dirty)
  * apps/desktop/src-tauri/Cargo.toml (dirty)
  * apps/desktop/src-tauri/src/main.rs (dirty)
  * apps/desktop/src/App.tsx (dirty)
  * apps/desktop/tsconfig.json (dirty)
  * Kno Yoo V2 开发节奏计划（20周）.docx (dirty)
  * KnoYoo PRD V2.docx (dirty)
  * package.json (dirty)
  * pnpm-lock.yaml (dirty)
  * ~$o Yoo V2 开发节奏计划（20周）.docx (dirty)
  * 已废弃的旧文件/ (dirty)
  * 开发备忘录.txt (dirty)
  * 开发日志.txt (dirty)
  * 知遇（KnoYoo）商业计划书.docx (dirty)
  * 项目开发计划/ (dirty)

这是正常的提示：cargo fix 默认不在“未提交状态”下改代码，怕把你本地改动覆盖掉。你有两种做法，选一种即可。


方案 A（推荐）：先提交一版，再运行 cargo fix
# 把当前改动全部纳入这次提交
git add -A
git commit -m "chore: checkpoint before cargo fix"

# 然后到 src-tauri 运行 cargo fix
cd .\src-tauri\
cargo fix --bin desktop



















